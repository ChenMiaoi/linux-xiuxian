---
title: 第一百八十六章：读写锁
---

# 第一百八十六章：读写锁

<CultivationRealm realm="大乘期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/locking/rwsem.c" />

## 一

林小源在互斥锁的修炼中，发现了更精细的锁——读写锁。

读写锁区分读和写。多个读者可以同时持锁，写者独占。适合读多写少的场景。

```c:playground:读写锁 — 读多写少的选择
#include <stdio.h>
#include <pthread.h>

/*
 * 读写锁:
 *
 * 类型:
 *   rwlock_t — 自旋读写锁
 *   rw_semaphore — 读写信号量
 *
 * 操作:
 *   read_lock() — 获取读锁
 *   read_unlock() — 释放读锁
 *   write_lock() — 获取写锁
 *   write_unlock() — 释放写锁
 *
 * 规则:
 *   多个读者可以同时持锁
 *   写者独占
 *   读者和写者互斥
 *
 * 优势:
 *   读并发
 *   适合读多写少
 *
 * 问题:
 *   写者可能饥饿
 *   读者太多，写者无法获取锁
 *
 * 解决方案:
 *   写者优先
 *   读写公平
 */

pthread_rwlock_t rwlock = PTHREAD_RWLOCK_INITIALIZER;
int shared_data = 0;

void *reader(void *arg) {
    int id = *(int *)arg;

    pthread_rwlock_rdlock(&rwlock);
    printf("读者 %d: 读取 shared_data = %d\n", id, shared_data);
    pthread_rwlock_unlock(&rwlock);

    return NULL;
}

void *writer(void *arg) {
    int id = *(int *)arg;

    pthread_rwlock_wrlock(&rwlock);
    shared_data++;
    printf("写者 %d: 写入 shared_data = %d\n", id, shared_data);
    pthread_rwlock_unlock(&rwlock);

    return NULL;
}

int main() {
    printf("=== 读写锁 — 读多写少的选择 ===\n\n");

    pthread_t threads[6];
    int ids[] = {1, 2, 3, 4, 5, 6};

    printf("--- 并发读 ---\n");
    for (int i = 0; i < 3; i++) {
        pthread_create(&threads[i], NULL, reader, &ids[i]);
    }
    for (int i = 0; i < 3; i++) {
        pthread_join(threads[i], NULL);
    }

    printf("\n--- 写者 ---\n");
    pthread_create(&threads[3], NULL, writer, &ids[3]);
    pthread_join(threads[3], NULL);

    printf("\n--- 读写锁规则 ---\n");
    printf("读者:\n");
    printf("  多个读者可以同时持锁\n");
    printf("  读并发\n\n");
    printf("写者:\n");
    printf("  写者独占\n");
    printf("  读者和写者互斥\n\n");

    printf("--- 读写锁类型 ---\n");
    printf("rwlock_t:\n");
    printf("  自旋读写锁\n");
    printf("  中断上下文使用\n\n");
    printf("rw_semaphore:\n");
    printf("  读写信号量\n");
    printf("  进程上下文使用\n\n");

    printf("--- 写者饥饿 ---\n");
    printf("问题:\n");
    printf("  读者太多\n");
    printf("  写者无法获取锁\n\n");
    printf("解决:\n");
    printf("  写者优先\n");
    printf("  新读者等待\n\n");

    printf("--- 使用场景 ---\n");
    printf("1. 路由表:\n");
    printf("   读取频繁\n");
    printf("   修改稀少\n\n");
    printf("2. 配置数据:\n");
    printf("   读取频繁\n");
    printf("   偶尔更新\n\n");
    printf("3. 文件系统元数据:\n");
    printf("   读取频繁\n");
    printf("   修改较少\n");

    pthread_rwlock_destroy(&rwlock);
    return 0;
}
```

*原来"读写锁"是"并发"的"精细控制"。*

---

## 二

林小源在研究读写锁的过程中，理解了写者饥饿问题。

如果读者持续不断，写者可能永远获取不到锁。解决方案是写者优先——当有写者等待时，新读者等待。

*原来"公平"是"设计"的"挑战"。*

---

## 三

林小源在观察读写锁的选择中，理解了它与 RCU 的关系。

RCU 比读写锁更高效——读取完全无锁。但 RCU 的写入开销更大。选择哪种取决于读写比例。

*原来"选择"是"权衡"的"艺术"。*

---

### 道藏笔记

::: tip 内核启示
**读写锁区分读和写操作。**

规则：
- 多个读者可以同时持锁
- 写者独占
- 读者和写者互斥

类型：
- rwlock_t — 自旋读写锁
- rw_semaphore — 读写信号量

写者饥饿：
- 读者太多，写者等待
- 解决：写者优先

使用场景：
- 读多写少
- 路由表、配置数据

*读写锁是精细控制——读并发，写独占。*
:::

---

<ChapterGate
  id="rwlock"
  title="读写锁之试"
  prompt="读写锁有什么规则？什么是写者饥饿？"
  answer="规则：多读者可同时持锁，写者独占，读写互斥。写者饥饿是读者太多导致写者无法获取锁，用写者优先解决"
  placeholder="描述读写锁"
/>

<ChapterNav prev="/novel/vol9-architecture/ch185" next="/novel/vol9-architecture/ch187" />
