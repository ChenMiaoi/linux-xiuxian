---
title: 第一百七十七章：读取侧临界区
---

# 第一百七十七章：读取侧临界区

<CultivationRealm realm="大乘期" />

> **涉及内核源码：** <KernelSourceLink path="include/linux/rcupdate.h" />

## 一

林小源在 RCU 的指引下，深入理解了读取侧临界区。

读取侧临界区是 RCU 的基础——读取者在临界区内访问 RCU 保护的数据。临界区由 rcu_read_lock() 和 rcu_read_unlock() 界定。

```c:playground:读取侧临界区 — RCU 的基础
#include <stdio.h>
#include <stdlib.h>

/*
 * 读取侧临界区:
 *
 * rcu_read_lock()
 *   进入临界区
 *   告诉内核: 我在读取
 *   实际上是禁止抢占
 *
 * rcu_read_unlock()
 *   离开临界区
 *   告诉内核: 我读完了
 *   实际上是允许抢占
 *
 * 临界区内的约束:
 *   不能睡眠
 *   不能调用可能睡眠的函数
 *   不能持有导致睡眠的锁
 *
 * rcu_dereference():
 *   读取 RCU 保护的指针
 *   确保读取顺序正确
 *   配合内存屏障使用
 *
 * 线程 1 (读取者):        线程 2 (写入者):
 *   rcu_read_lock()         new = kmalloc()
 *   p = rcu_dereference()   *new = *old
 *   使用 p                  rcu_assign_pointer()
 *   rcu_read_unlock()       synchronize_rcu()
 *                           kfree(old)
 */

/* 模拟 RCU 保护的链表 */
struct node {
    int data;
    struct node *next;
};

struct node *global_head = NULL;

/* 模拟 rcu_read_lock — 禁止抢占 */
int in_rcu_read = 0;

void rcu_read_lock(void) {
    in_rcu_read = 1;
}

void rcu_read_unlock(void) {
    in_rcu_read = 0;
}

/* 读取临界区内的操作 */
void rcu_reader(void) {
    printf("读取者:\n");
    rcu_read_lock();
    printf("  进入临界区 (禁止抢占)\n");

    struct node *p = global_head;
    while (p) {
        printf("  读取: %d\n", p->data);
        p = p->next;
    }

    printf("  离开临界区 (允许抢占)\n");
    rcu_read_unlock();
}

/* 写入者的操作 */
void rcu_writer(int new_data) {
    printf("写入者:\n");

    /* 复制 */
    struct node *new = malloc(sizeof(struct node));
    new->data = new_data;
    new->next = global_head;
    printf("  创建新节点: %d\n", new_data);

    /* 更新指针 */
    struct node *old = global_head;
    global_head = new;
    printf("  更新指针\n");

    /* 等待宽限期 */
    printf("  等待宽限期...\n");
    /* synchronize_rcu() */

    /* 释放旧数据 */
    if (old) {
        printf("  释放旧节点: %d\n", old->data);
        free(old);
    }
}

int main() {
    printf("=== 读取侧临界区 — RCU 的基础 ===\n\n");

    /* 初始化 */
    rcu_writer(1);
    rcu_writer(2);

    printf("\n--- 读取 ---\n");
    rcu_reader();

    printf("\n--- 临界区约束 ---\n");
    printf("不能:\n");
    printf("  睡眠\n");
    printf("  调用可能睡眠的函数\n");
    printf("  持有导致睡眠的锁\n\n");
    printf("原因:\n");
    printf("  临界区内禁止抢占\n");
    printf("  睡眠会导致死锁\n\n");

    printf("--- rcu_dereference ---\n");
    printf("作用:\n");
    printf("  读取 RCU 保护的指针\n");
    printf("  确保读取顺序正确\n\n");
    printf("实现:\n");
    printf("  依赖内存屏障\n");
    printf("  防止编译器优化\n\n");

    printf("--- 多读者场景 ---\n");
    printf("线程 1: rcu_read_lock() ... rcu_read_unlock()\n");
    printf("线程 2: rcu_read_lock() ... rcu_read_unlock()\n");
    printf("线程 3: rcu_read_lock() ... rcu_read_unlock()\n");
    printf("写入者: 等待所有读者离开\n");

    /* 清理 */
    struct node *p = global_head;
    while (p) {
        struct node *next = p->next;
        free(p);
        p = next;
    }

    return 0;
}
```

*原来"临界区"是"读取"的"边界"。*

---

## 二

林小源在研究读取侧临界区的过程中，理解了它为什么不能睡眠。

RCU 读取侧临界区通过禁止抢占实现。如果在临界区内睡眠，会导致 CPU 被占用，其他线程无法运行，宽限期无法结束。

*原来"禁止睡眠"是"效率"的"保障"。*

---

## 三

林小源在观察多读者场景的过程中，理解了 RCU 的并发优势。

多个读者可以同时进入临界区，互不影响。写入者需要等待所有读者离开。这让读取的并发性能极好。

*原来"并发"是"RCU"的"力量"。*

---

### 道藏笔记

::: tip 内核启示
**读取侧临界区是 RCU 的基础。**

临界区：
- rcu_read_lock() — 进入
- rcu_read_unlock() — 离开
- rcu_dereference() — 读取指针

约束：
- 不能睡眠
- 不能调用可能睡眠的函数
- 不能持有导致睡眠的锁

实现：
- 禁止抢占
- 多读者可并发

*临界区是边界——读取在边界内完成。*
:::

---

<ChapterGate
  id="rcu-read-side"
  title="读取侧临界区之试"
  prompt="读取侧临界区有哪些约束？为什么不能睡眠？"
  answer="约束：不能睡眠、不能调用可能睡眠的函数、不能持有导致睡眠的锁。不能睡眠因为临界区禁止抢占，睡眠会导致死锁"
  placeholder="描述 RCU 读取侧"
/>

<ChapterNav prev="/novel/vol9-architecture/ch176" next="/novel/vol9-architecture/ch178" />
