---
title: 第一百七十九章：回调与屏障
---

# 第一百七十九章：回调与屏障

<CultivationRealm realm="大乘期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/rcu/tree.c" />

## 一

林小源在宽限期的修炼中，理解了 RCU 回调和内存屏障的机制。

call_rcu 注册回调函数，宽限期结束后自动调用。内存屏障确保操作顺序正确。

```c:playground:回调与屏障 — RCU 的高级机制
#include <stdio.h>
#include <stdlib.h>

/*
 * RCU 回调:
 *
 * call_rcu(head, func):
 *   注册回调函数
 *   宽限期结束后调用 func(head)
 *   不阻塞当前线程
 *
 * rcu_barrier():
 *   等待所有回调完成
 *   类似 synchronize_rcu
 *   但等待的是回调
 *
 * 回调链:
 *   内核维护回调链表
 *   宽限期结束后批量执行
 *   避免频繁的上下文切换
 *
 * 内存屏障:
 *   smp_wmb() — 写屏障
 *   smp_rmb() — 读屏障
 *   smp_mb() — 全屏障
 *
 * RCU 使用的屏障:
 *   rcu_assign_pointer — 包含写屏障
 *   rcu_dereference — 包含读屏障
 */

/* 模拟 RCU 回调 */
struct rcu_head {
    void (*func)(struct rcu_head *);
    struct rcu_head *next;
};

struct rcu_callback_list {
    struct rcu_head *head;
    struct rcu_head **tail;
    int count;
};

void callback_list_init(struct rcu_callback_list *list) {
    list->head = NULL;
    list->tail = &list->head;
    list->count = 0;
}

void call_rcu(struct rcu_callback_list *list, struct rcu_head *head,
              void (*func)(struct rcu_head *)) {
    head->func = func;
    head->next = NULL;
    *list->tail = head;
    list->tail = &head->next;
    list->count++;
}

void process_callbacks(struct rcu_callback_list *list) {
    printf("处理 %d 个回调:\n", list->count);
    struct rcu_head *p = list->head;
    while (p) {
        struct rcu_head *next = p->next;
        p->func(p);
        p = next;
    }
    list->head = NULL;
    list->tail = &list->head;
    list->count = 0;
}

/* 示例回调函数 */
void free_callback(struct rcu_head *head) {
    printf("  回调: 释放内存\n");
}

int main() {
    printf("=== 回调与屏障 — RCU 的高级机制 ===\n\n");

    /* RCU 回调 */
    struct rcu_callback_list callbacks;
    callback_list_init(&callbacks);

    struct rcu_head head1, head2;
    call_rcu(&callbacks, &head1, free_callback);
    call_rcu(&callbacks, &head2, free_callback);

    printf("--- RCU 回调 ---\n");
    printf("注册了 %d 个回调\n", callbacks.count);
    printf("宽限期结束后自动调用\n\n");

    printf("--- 模拟宽限期结束 ---\n");
    process_callbacks(&callbacks);

    printf("\n--- call_rcu vs synchronize_rcu ---\n");
    printf("call_rcu:\n");
    printf("  异步\n");
    printf("  注册回调\n");
    printf("  不阻塞\n\n");
    printf("synchronize_rcu:\n");
    printf("  同步\n");
    printf("  阻塞等待\n");

    printf("\n--- rcu_barrier ---\n");
    printf("作用:\n");
    printf("  等待所有回调完成\n\n");
    printf("使用场景:\n");
    printf("  模块卸载时\n");
    printf("  确保所有 RCU 操作完成\n\n");

    printf("\n--- 内存屏障 ---\n");
    printf("smp_wmb():\n");
    printf("  写屏障\n");
    printf("  确保写入顺序\n\n");
    printf("smp_rmb():\n");
    printf("  读屏障\n");
    printf("  确保读取顺序\n\n");
    printf("smp_mb():\n");
    printf("  全屏障\n");
    printf("  确保所有操作顺序\n\n");

    printf("--- RCU 中的屏障 ---\n");
    printf("rcu_assign_pointer:\n");
    printf("  包含写屏障\n");
    printf("  确保数据写入在指针更新前\n\n");
    printf("rcu_dereference:\n");
    printf("  包含读屏障\n");
    printf("  确保指针读取在数据访问前\n");

    return 0;
}
```

*原来"回调"是"异步"的"智慧"。*

---

## 二

林小源在研究 RCU 回调的过程中，理解了回调链的设计。

内核维护回调链表，宽限期结束后批量执行。这避免了频繁的上下文切换，提高了效率。

*原来"批量"是"效率"的"来源"。*

---

## 三

林小源在观察内存屏障的过程中，理解了它在 RCU 中的作用。

rcu_assign_pointer 包含写屏障，确保数据写入在指针更新前。rcu_dereference 包含读屏障，确保指针读取在数据访问前。这保证了数据一致性。

*原来"屏障"是"顺序"的"保障"。*

---

### 道藏笔记

::: tip 内核启示
**RCU 回调和内存屏障是高级机制。**

RCU 回调：
- call_rcu — 注册回调
- 宽限期结束后自动调用
- 回调链批量执行

rcu_barrier：
- 等待所有回调完成
- 用于模块卸载

内存屏障：
- smp_wmb — 写屏障
- smp_rmb — 读屏障
- smp_mb — 全屏障

RCU 中的屏障：
- rcu_assign_pointer — 写屏障
- rcu_dereference — 读屏障

*回调是异步的智慧，屏障是顺序的保障。*
:::

---

<ChapterGate
  id="rcu-callbacks"
  title="回调与屏障之试"
  prompt="call_rcu 和 rcu_barrier 有什么作用？内存屏障在 RCU 中如何使用？"
  answer="call_rcu 注册回调，宽限期后自动调用；rcu_barrier 等待所有回调完成。rcu_assign_pointer 包含写屏障，rcu_dereference 包含读屏障"
  placeholder="描述 RCU 回调"
/>

<ChapterNav prev="/novel/vol9-architecture/ch178" next="/novel/vol9-architecture/ch180" />
