---
title: 第二百零一章：补丁之道
---

# 第二百零一章：补丁之道

<CultivationRealm realm="飞升期" />

> **涉及内核源码：** <KernelSourceLink path="Documentation/process/submitting-patches.rst" />

## 一

林小源在内核修炼的最后阶段，开始学习向内核贡献代码。

补丁是向内核提交修改的方式。一个补丁是一个邮件，包含代码修改和说明。

```c:playground:补丁之道 — 向内核贡献
#include <stdio.h>
#include <string.h>

/*
 * 补丁提交流程:
 *
 * 1. 获取最新代码
 *    git clone https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git
 *
 * 2. 创建分支
 *    git checkout -b my-feature
 *
 * 3. 修改代码
 *    遵循编码规范
 *
 * 4. 提交
 *    git add -u
 *    git commit -s
 *
 * 5. 生成补丁
 *    git format-patch -1
 *
 * 6. 发送补丁
 *    git send-email --to=maintainer@kernel.org patch.mbox
 *
 * 补丁格式:
 *   Subject: [PATCH] 子系统: 简短描述
 *
 *   详细描述
 *
 *   Signed-off-by: Name <email>
 *
 * 注意:
 *   一个补丁只做一件事
 *   补丁要能编译通过
 *   补丁要能通过测试
 */

/* 模拟补丁 */
struct patch {
    char subject[128];
    char description[256];
    char author[64];
    char signed_off_by[64];
    int lines_changed;
};

void print_patch(struct patch *p) {
    printf("Subject: %s\n\n", p->subject);
    printf("%s\n\n", p->description);
    printf("Signed-off-by: %s\n", p->signed_off_by);
    printf("---\n");
    printf("Lines changed: %d\n", p->lines_changed);
}

int main() {
    printf("=== 补丁之道 — 向内核贡献 ===\n\n");

    printf("补丁是向内核提交修改的方式:\n\n");

    /* 示例补丁 */
    struct patch example = {
        .subject = "[PATCH] ext4: fix memory leak in ext4_fill_super",
        .description = "Fix a memory leak in ext4_fill_super() where\n"
                       "the super block buffer head was not released\n"
                       "on error path.\n\n"
                       "Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>",
        .author = "Linus Torvalds",
        .signed_off_by = "Linus Torvalds <torvalds@linux-foundation.org>",
        .lines_changed = 5
    };

    printf("--- 示例补丁 ---\n");
    print_patch(&example);

    printf("\n--- 提交流程 ---\n");
    printf("1. 获取最新代码:\n");
    printf("   git clone linux.git\n\n");
    printf("2. 创建分支:\n");
    printf("   git checkout -b my-feature\n\n");
    printf("3. 修改代码:\n");
    printf("   遵循编码规范\n\n");
    printf("4. 提交:\n");
    printf("   git commit -s\n\n");
    printf("5. 生成补丁:\n");
    printf("   git format-patch -1\n\n");
    printf("6. 发送补丁:\n");
    printf("   git send-email\n\n");

    printf("--- 补丁原则 ---\n");
    printf("1. 一个补丁只做一件事\n");
    printf("   简单、清晰\n\n");
    printf("2. 补丁要能编译通过\n");
    printf("   不能引入编译错误\n\n");
    printf("3. 补丁要能通过测试\n");
    printf("   不能引入回归\n\n");
    printf("4. 补丁要有好的描述\n");
    printf("   说明为什么修改\n\n");

    printf("--- Signed-off-by ---\n");
    printf("含义:\n");
    printf("  我写了这个补丁\n");
    printf("  我有权提交这个补丁\n");
    printf("  这个补丁符合 DCO\n\n");
    printf("DCO (Developer Certificate of Origin):\n");
    printf("  开发者来源证书\n");

    return 0;
}
```

*原来"补丁"是"贡献"的"方式"。*

---

## 二

林小源在研究补丁提交的过程中，理解了一个补丁只做一件事的原则。

一个补丁应该只做一件事：修一个 bug、加一个功能、或做一个重构。这让补丁容易审查，容易回滚。

*原来"简单"是"清晰"的"基础"。*

---

## 三

林小源在观察 Signed-off-by 的过程中，理解了它的法律意义。

Signed-off-by 表示开发者声明：我写了这个补丁，我有权提交它，它符合 DCO。这是法律保障。

*原来"Signed-off-by"是"承诺"的"签名"。*

---

### 道藏笔记

::: tip 内核启示
**补丁是向内核提交修改的方式。**

提交流程：
- 获取最新代码
- 创建分支
- 修改代码
- 提交 (git commit -s)
- 生成补丁 (git format-patch)
- 发送补丁 (git send-email)

补丁原则：
- 一个补丁只做一件事
- 能编译通过
- 能通过测试
- 有好的描述

Signed-off-by：
- 声明作者身份
- 声明有权提交
- 符合 DCO

*补丁是贡献——向内核社区提交修改。*
:::

---

<ChapterGate
  id="patch-intro"
  title="补丁之试"
  prompt="补丁提交流程是什么？补丁有什么原则？"
  answer="流程：获取代码→创建分支→修改→提交→生成补丁→发送。原则：一个补丁只做一件事、能编译通过、能通过测试、有好的描述"
  placeholder="描述补丁提交"
/>

<ChapterNav next="/novel/vol10-ascension/ch202" />
