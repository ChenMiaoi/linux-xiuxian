---
title: 第九十七章：文件锁
---

# 第九十七章：文件锁

<CultivationRealm realm="斩灵期" />

> **涉及内核源码：** <KernelSourceLink path="fs/locks.c" line="fcntl_setlk" />

## 一

林小源在研究文件系统的过程中，发现了一个并发问题：多个进程同时读写同一个文件怎么办？

文件锁是解决这个问题的机制。它允许多个进程共享读取，但独占写入——避免数据竞争。

```c:playground:文件锁 — 并发访问控制
#include <stdio.h>

/*
 * 文件锁的类型：
 *
 * 1. 共享锁 (F_RDLCK)
 *    多个进程可以同时持有
 *    用于读取
 *
 * 2. 独占锁 (F_WRLCK)
 *    只有一个进程可以持有
 *    用于写入
 *
 * 3. 解锁 (F_UNLCK)
 *    释放锁
 *
 * 锁的粒度：
 *   记录锁: 锁定文件的某个区域
 *   整个文件: 锁定整个文件
 *
 * fcntl 锁:
 *   fcntl(fd, F_SETLK, &lock)
 *   fcntl(fd, F_GETLK, &lock)
 *
 * flock 锁:
 *   flock(fd, LOCK_SH)
 *   flock(fd, LOCK_EX)
 */

struct file_lock {
    int type;       /* F_RDLCK, F_WRLCK, F_UNLCK */
    int start;      /* 起始偏移 */
    int length;     /* 长度 */
    int pid;        /* 持有者 */
};

int main() {
    printf("=== 文件锁 — 并发访问控制 ===\n\n");

    printf("文件锁的类型:\n\n");

    printf("1. 共享锁 (F_RDLCK):\n");
    printf("   多个进程可以同时持有\n");
    printf("   用于读取\n\n");

    printf("2. 独占锁 (F_WRLCK):\n");
    printf("   只有一个进程可以持有\n");
    printf("   用于写入\n\n");

    printf("3. 解锁 (F_UNLCK):\n");
    printf("   释放锁\n\n");

    printf("--- 锁的兼容性 ---\n");
    printf("请求 \\ 持有  共享锁  独占锁\n");
    printf("共享锁       允许    拒绝\n");
    printf("独占锁       拒绝    拒绝\n\n");

    printf("--- 记录锁 ---\n");
    printf("锁可以锁定文件的某个区域:\n");
    printf("  进程 A: 锁定 0-100\n");
    printf("  进程 B: 锁定 100-200\n");
    printf("  两者不冲突\n\n");

    printf("--- fcntl 锁 ---\n");
    printf("struct flock lock = {\n");
    printf("  .l_type = F_WRLCK,\n");
    printf("  .l_whence = SEEK_SET,\n");
    printf("  .l_start = 0,\n");
    printf("  .l_len = 0,  /* 整个文件 */\n");
    printf("};\n");
    printf("fcntl(fd, F_SETLK, &lock);\n\n");

    printf("--- flock 锁 ---\n");
    printf("flock(fd, LOCK_SH);  /* 共享锁 */\n");
    printf("flock(fd, LOCK_EX);  /* 独占锁 */\n");
    printf("flock(fd, LOCK_UN);  /* 解锁 */\n\n");

    printf("--- 死锁检测 ---\n");
    printf("内核检测死锁:\n");
    printf("  进程 A 等待进程 B 释放锁\n");
    printf("  进程 B 等待进程 A 释放锁\n");
    printf("  内核拒绝其中一个请求\n");

    return 0;
}
```

*原来"锁"是"秩序"的"守护者"。*

---

## 二

林小源在研究文件锁的过程中，理解了 fcntl 和 flock 的区别。

`flock()` 锁定整个文件——简单但粗粒度。`fcntl()` 可以锁定文件的某个区域——更灵活但更复杂。

*原来"粒度"决定了"灵活性"。*

---

## 三

林小源在观察文件锁的过程中，理解了强制锁和建议锁的区别。

建议锁（Advisory Lock）只对使用锁的进程有效——其他进程可以忽略锁直接访问文件。强制锁（Mandatory Lock）对所有进程有效——内核会阻止违反锁的访问。

*原来"强制"和"建议"是两种"力度"。*

---

### 道藏笔记

::: tip 内核启示
**文件锁控制多个进程对文件的并发访问。**

文件锁的类型：
- 共享锁（F_RDLCK）— 多个进程同时持有，用于读取
- 独占锁（F_WRLCK）— 只有一个进程持有，用于写入
- 解锁（F_UNLCK）— 释放锁

锁的粒度：
- `flock()` — 锁定整个文件
- `fcntl()` — 锁定文件的某个区域

建议锁 vs 强制锁：
- 建议锁 — 只对使用锁的进程有效
- 强制锁 — 对所有进程有效

*文件锁是"秩序"的守护者——让并发访问有序进行。*
:::

---

<ChapterGate
  id="file-lock"
  title="文件锁之试"
  prompt="共享锁和独占锁有什么区别？fcntl 和 flock 有什么区别？"
  answer="共享锁允许多个进程同时持有用于读取，独占锁只允许一个进程持有用于写入。flock 锁定整个文件，fcntl 可以锁定文件的某个区域"
  placeholder="描述文件锁"
/>

<ChapterNav prev="/novel/vol5-filesystem/ch96" next="/novel/vol5-filesystem/ch98" />
