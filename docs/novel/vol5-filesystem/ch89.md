---
title: 第八十九章：btrfs 之术
---

# 第八十九章：btrfs 之术

<CultivationRealm realm="斩灵期" />

> **涉及内核源码：** <KernelSourceLink path="fs/btrfs/ctree.c" />

## 一

林小源在森林深处发现了另一棵古树——btrfs。

btrfs 是新一代的文件系统，采用 Copy-on-Write（COW）机制——每次修改都写到新位置，不覆盖原有数据。这带来了快照、校验和、压缩等高级特性。

```c:playground:btrfs — COW 文件系统
#include <stdio.h>

/*
 * btrfs 的核心特性：
 *
 * 1. Copy-on-Write (COW)
 *    修改数据时写到新位置
 *    不覆盖原有数据
 *    快照的基础
 *
 * 2. B-tree 存储
 *    所有数据和元数据都用 B-tree 组织
 *    包括文件数据、inode、目录
 *
 * 3. 校验和
 *    每个数据块都有校验和
 *    检测数据损坏
 *
 * 4. 压缩
 *    支持透明压缩
 *    zlib, lzo, zstd
 *
 * 5. 子卷
 *    文件系统可以有多个子卷
 *    每个子卷可以独立挂载
 *
 * 6. 快照
 *    基于 COW 的快照
 *    几乎不占用额外空间
 */

int main() {
    printf("=== btrfs — COW 文件系统 ===\n\n");

    printf("btrfs vs ext4:\n");
    printf("  ext4:\n");
    printf("    原地修改\n");
    printf("    日志保证一致性\n");
    printf("    成熟稳定\n\n");
    printf("  btrfs:\n");
    printf("    COW（写时复制）\n");
    printf("    校验和保证数据完整性\n");
    printf("    支持快照、压缩、子卷\n\n");

    printf("--- COW 工作原理 ---\n");
    printf("修改前:\n");
    printf("  原始数据块 A\n\n");
    printf("修改时:\n");
    printf("  分配新块 B\n");
    printf("  把修改写到 B\n");
    printf("  更新指针指向 B\n\n");
    printf("修改后:\n");
    printf("  原始块 A 保留（快照使用）\n");
    printf("  新数据在块 B\n\n");

    printf("--- B-tree 组织 ---\n");
    printf("btrfs 的 B-tree:\n");
    printf("  root\n");
    printf("  ├── inode items\n");
    printf("  ├── file data extents\n");
    printf("  ├── directory items\n");
    printf("  └── checksums\n\n");

    printf("--- 校验和 ---\n");
    printf("每个数据块有校验和:\n");
    printf("  数据块 + CRC32C 校验和\n");
    printf("  读取时验证\n");
    printf("  检测静默数据损坏\n\n");

    printf("--- 压缩 ---\n");
    printf("透明压缩:\n");
    printf("  压缩算法: zlib, lzo, zstd\n");
    printf("  自动压缩/解压\n");
    printf("  节省磁盘空间\n");

    return 0;
}
```

*原来"新树"的"根"是 COW。*

---

## 二

林小源在研究 btrfs 的过程中，理解了校验和的价值。

传统文件系统不验证数据的完整性——如果磁盘上的数据损坏，文件系统不知道。btrfs 为每个数据块计算校验和——读取时验证，可以检测静默数据损坏。

*原来"完整性"比"存在"更重要。*

---

## 三

林小源在观察 btrfs 的过程中，理解了它的发展。

btrfs 仍在发展中——某些功能（如 RAID 5/6）还不稳定。但对于需要快照、校验和、压缩的场景，btrfs 是很好的选择。

*原来"新"需要"时间"来成熟。*

---

### 道藏笔记

::: tip 内核启示
**btrfs 是基于 COW 的新一代文件系统。**

btrfs 的核心特性：
- COW — 修改时写到新位置
- B-tree — 所有数据用 B-tree 组织
- 校验和 — 检测数据损坏
- 压缩 — 透明压缩（zlib, lzo, zstd）
- 子卷 — 多个独立的子文件系统
- 快照 — 基于 COW 的瞬时快照

btrfs vs ext4：
- ext4 — 原地修改，日志保证一致性
- btrfs — COW，校验和保证完整性

*btrfs 是"未来"的文件系统——COW 带来无限可能。*
:::

---

<ChapterGate
  id="btrfs"
  title="btrfs 之试"
  prompt="btrfs 的 COW 机制如何工作？校验和有什么作用？"
  answer="COW 在修改数据时写到新位置，不覆盖原有数据，这是快照的基础。校验和检测静默数据损坏，保证数据完整性"
  placeholder="描述 btrfs 的特性"
/>

<ChapterNav prev="/novel/vol5-filesystem/ch88" next="/novel/vol5-filesystem/ch90" />
