---
title: 第九十二章：sysfs
---

# 第九十二章：sysfs

<CultivationRealm realm="斩灵期" />

> **涉及内核源码：** <KernelSourceLink path="fs/sysfs/dir.c" />

## 一

林小源在森林中发现了另一棵特殊的树——sysfs。

sysfs 是设备模型的文件系统表示。它把内核中的设备、驱动、总线等信息以目录和文件的形式暴露给用户空间。

```c:playground:sysfs — 设备模型的文件系统
#include <stdio.h>

/*
 * sysfs 的结构：
 *
 *   /sys/
 *   ├── block/        — 块设备
 *   ├── bus/          — 总线类型
 *   ├── class/        — 设备类
 *   ├── devices/      — 所有设备
 *   ├── firmware/     — 固件接口
 *   ├── fs/           — 文件系统
 *   ├── kernel/       — 内核信息
 *   ├── module/       — 内核模块
 *   └── power/        — 电源管理
 *
 * sysfs vs procfs:
 *   procfs — 进程和内核信息
 *   sysfs — 设备和驱动信息
 *
 * sysfs 的特点：
 *   - 一个文件一个值
 *   - 每个目录对应一个 kobject
 *   - 自动生成
 */

int main() {
    printf("=== sysfs — 设备模型的文件系统 ===\n\n");

    printf("sysfs 目录结构:\n");
    printf("  /sys/\n");
    printf("  ├── block/        块设备\n");
    printf("  │   └── sda/\n");
    printf("  │       ├── size\n");
    printf("  │       └── queue/\n");
    printf("  ├── bus/          总线类型\n");
    printf("  │   ├── pci/\n");
    printf("  │   └── usb/\n");
    printf("  ├── class/        设备类\n");
    printf("  │   ├── net/\n");
    printf("  │   └── block/\n");
    printf("  ├── devices/      所有设备\n");
    printf("  ├── firmware/     固件接口\n");
    printf("  ├── kernel/       内核信息\n");
    printf("  ├── module/       内核模块\n");
    printf("  └── power/        电源管理\n\n");

    printf("--- 设备信息示例 ---\n");
    printf("/sys/block/sda/:\n");
    printf("  size: 1953525168 (扇区数)\n");
    printf("  removable: 0\n");
    printf("  ro: 0 (只读标志)\n\n");

    printf("/sys/class/net/eth0/:\n");
    printf("  address: 00:11:22:33:44:55\n");
    printf("  mtu: 1500\n");
    printf("  speed: 1000 (Mbps)\n\n");

    printf("--- kobject 与 sysfs ---\n");
    printf("每个 kobject 对应一个目录:\n");
    printf("  struct kobject {\n");
    printf("    char *name;  // 目录名\n");
    printf("    ...          // 其他字段\n");
    printf("  };\n\n");
    printf("kobject 的属性对应文件:\n");
    printf("  struct kobj_attribute {\n");
    printf("    char *name;       // 文件名\n");
    printf("    ssize_t (*show)(); // 读取函数\n");
    printf"    ssize_t (*store)(); // 写入函数\n");
    printf("  };\n\n");

    printf("--- sysfs 的用途 ---\n");
    printf("1. 查看设备信息\n");
    printf("   cat /sys/block/sda/size\n\n");
    printf("2. 修改设备参数\n");
    printf("   echo 1 > /sys/class/leds/led0/brightness\n\n");
    printf("3. udev 规则\n");
    printf("   根据 sysfs 信息创建设备节点\n");

    return 0;
}
```

*原来"设备"也有"身份证"。*

---

## 二

林小源在研究 sysfs 的过程中，理解了 kobject 的作用。

kobject 是 Linux 设备模型的核心——每个设备、驱动、总线都对应一个 kobject。sysfs 根据 kobject 自动生成目录和文件。

*原来"kobject"是"设备"的"灵魂"。*

---

## 三

林小源在观察 sysfs 的过程中，理解了它与 udev 的关系。

udev 是用户空间的设备管理器——它根据 sysfs 中的信息自动创建设备节点。当设备插入时，内核在 sysfs 中创建对应目录，udev 收到事件后创建 `/dev/` 下的设备节点。

*原来"设备节点"是"自动"创建的。*

---

### 道藏笔记

::: tip 内核启示
**sysfs 是设备模型的文件系统表示。**

sysfs 的结构：
- `/sys/block/` — 块设备
- `/sys/bus/` — 总线类型
- `/sys/class/` — 设备类
- `/sys/devices/` — 所有设备

sysfs 的特点：
- 一个文件一个值
- 每个目录对应一个 kobject
- 自动生成

sysfs vs procfs：
- procfs — 进程和内核信息
- sysfs — 设备和驱动信息

*sysfs 是"设备"的"身份证"——让设备信息可见。*
:::

---

<ChapterGate
  id="sysfs"
  title="sysfs 之试"
  prompt="sysfs 和 procfs 有什么区别？kobject 在 sysfs 中的作用是什么？"
  answer="procfs 暴露进程和内核信息，sysfs 暴露设备和驱动信息。每个 kobject 对应 sysfs 中的一个目录，kobject 的属性对应目录中的文件"
  placeholder="描述 sysfs"
/>

<ChapterNav prev="/novel/vol5-filesystem/ch91" next="/novel/vol5-filesystem/ch93" />
