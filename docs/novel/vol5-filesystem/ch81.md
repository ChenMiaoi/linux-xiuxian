---
title: 第八十一章：古树之森
---

# 第八十一章：古树之森

<CultivationRealm realm="斩灵期" />

> **涉及内核源码：** <KernelSourceLink path="include/linux/fs.h" />

## 一

林小源踏入了文件系统的世界——一片古老的森林。

在这片森林中，每棵树都是一个文件，每条路都是一个目录。但森林之下，有一个统一的抽象层——VFS（Virtual File System）。无论上面是 ext4、btrfs 还是 procfs，VFS 都为它们提供相同的接口。

```c:playground:VFS — 虚拟文件系统
#include <stdio.h>

/*
 * VFS 的四大对象：
 *
 * 1. superblock — 文件系统的元数据
 *    超级块，描述整个文件系统
 *    挂载时创建，卸载时销毁
 *
 * 2. inode — 文件的元数据
 *    索引节点，描述一个文件
 *    包含权限、大小、时间戳等
 *    不包含文件名
 *
 * 3. dentry — 目录项
 *    目录项，连接文件名和 inode
 *    组成目录树
 *    有缓存（dcache）
 *
 * 4. file — 打开的文件
 *    文件对象，描述一次打开
 *    包含读写位置、权限等
 *    进程关闭后销毁
 *
 * 关系：
 *   superblock 包含多个 inode
 *   dentry 连接文件名和 inode
 *   file 是进程打开的 inode
 */

int main() {
    printf("=== VFS — 虚拟文件系统 ===\n\n");

    printf("VFS 的四大对象:\n\n");

    printf("1. superblock（超级块）\n");
    printf("   描述整个文件系统\n");
    printf("   包含块大小、inode 数量、挂载点\n");
    printf("   挂载时创建\n\n");

    printf("2. inode（索引节点）\n");
    printf("   描述一个文件\n");
    printf("   包含权限、大小、时间戳\n");
    printf("   不包含文件名\n\n");

    printf("3. dentry（目录项）\n");
    printf("   连接文件名和 inode\n");
    printf("   组成目录树\n");
    printf("   有缓存（dcache）\n\n");

    printf("4. file（文件对象）\n");
    printf("   描述一次打开操作\n");
    printf("   包含读写位置\n");
    printf("   关闭后销毁\n\n");

    printf("--- 对象之间的关系 ---\n");
    printf("  superblock\n");
    printf("    ├── inode 1 (文件 a)\n");
    printf("    ├── inode 2 (文件 b)\n");
    printf("    └── inode 3 (目录 /home)\n");
    printf("          ├── dentry \"user\" → inode 4\n");
    printf("          └── dentry \"file\" → inode 5\n");
    printf("                └── file (进程打开)\n\n");

    printf("--- VFS 的意义 ---\n");
    printf("统一接口: open, read, write, close\n");
    printf("透明性: 用户不需要知道底层文件系统\n");
    printf("可扩展: 新文件系统只需实现 VFS 接口\n");

    return 0;
}
```

*原来"统一"是"抽象"的最高境界。*

---

## 二

林小源在研究 VFS 的过程中，理解了它的设计思想。

VFS 的核心是"接口"——它定义了一组操作（open、read、write、close），每个文件系统只需要实现这些接口。用户程序不需要关心底层是 ext4 还是 btrfs。

*原来"接口"是"解耦"的方式。*

---

## 三

林小源在观察 VFS 的过程中，理解了四大对象的生命周期。

superblock 在挂载时创建，卸载时销毁。inode 在文件存在时一直存在。dentry 有缓存，即使文件关闭也可能保留。file 在进程打开文件时创建，关闭时销毁。

*原来"生命周期"各不相同。*

---

### 道藏笔记

::: tip 内核启示
**VFS 是文件系统的统一抽象层。**

VFS 的四大对象：
- superblock — 文件系统的元数据
- inode — 文件的元数据（不含文件名）
- dentry — 目录项，连接文件名和 inode
- file — 打开的文件对象

VFS 的设计思想：
- 统一接口：open、read、write、close
- 透明性：用户不需要知道底层文件系统
- 可扩展：新文件系统只需实现接口

*VFS 是"至高的抽象"——让不同的文件系统看起来相同。*
:::

---

<ChapterGate
  id="vfs-objects"
  title="VFS 之试"
  prompt="VFS 的四大对象是什么？它们各自的作用是什么？"
  answer="superblock 描述文件系统元数据，inode 描述文件元数据，dentry 连接文件名和 inode，file 描述一次打开操作"
  placeholder="描述 VFS 的四大对象"
/>

<ChapterNav prev="/novel/vol4-memory/ch80" next="/novel/vol5-filesystem/ch82" />
