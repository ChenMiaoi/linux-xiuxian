---
title: 第一百五十七章：系统调用劫持
---

# 第一百五十七章：系统调用劫持

<CultivationRealm realm="渡劫期" />

> **涉及内核源码：** <KernelSourceLink path="arch/x86/kernel/entry_64.S" />

## 一

林小源在暗影深处，发现了 rootkit 最常用的技术——系统调用劫持。

系统调用是用户空间进入内核的唯一入口。劫持系统调用，就控制了用户程序与内核的所有交互。

```c:playground:系统调用劫持 — 控制入口
#include <stdio.h>
#include <string.h>

/*
 * 系统调用劫持的方法:
 *
 * 1. 修改 sys_call_table
 *    直接修改表中的函数指针
 *    需要解除写保护 (CR0)
 *
 * 2. inline hook
 *    在函数入口处注入跳转指令
 *    jmp malicious_function
 *
 * 3. ftrace hook
 *    利用 ftrace 框架
 *    在函数入口处插入回调
 *
 * 4. kprobe hook
 *    利用 kprobe 框架
 *    在任意地址设置探针
 */

/* 模拟 sys_call_table */
typedef int (*syscall_fn_t)(void);

struct {
    syscall_fn_t *table;
    int size;
} sys_call_table_obj = {
    .table = NULL,
    .size = 5,
};

syscall_fn_t original_table[5];
syscall_fn_t hooked_table[5];

/* 模拟原始系统调用 */
int sys_read_original(void) {
    return 100;  /* 返回读取的字节数 */
}

int sys_write_original(void) {
    return 200;  /* 返回写入的字节数 */
}

/* hook 后的系统调用 */
int sys_read_hooked(void) {
    printf("  [hook] 拦截 sys_read\n");
    printf("  [hook] 可以记录、修改、拒绝请求\n");
    /* 调用原始实现 */
    return sys_read_original();
}

int sys_write_hooked(void) {
    printf("  [hook] 拦截 sys_write\n");
    printf("  [hook] 可以过滤敏感数据\n");
    return sys_write_original();
}

int main() {
    printf("=== 系统调用劫持 — 控制入口 ===\n\n");

    /* 初始化原始表 */
    original_table[0] = sys_read_original;
    original_table[1] = sys_write_original;

    /* 初始化 hook 表 */
    hooked_table[0] = sys_read_hooked;
    hooked_table[1] = sys_write_hooked;

    printf("--- 原始系统调用 ---\n");
    printf("sys_read: %d\n", original_table[0]());
    printf("sys_write: %d\n", original_table[1]());

    printf("\n--- 安装 hook ---\n");
    printf("修改 sys_call_table[0] 指向 hook\n");
    printf("修改 sys_call_table[1] 指向 hook\n\n");

    printf("--- hook 后的系统调用 ---\n");
    hooked_table[0]();
    hooked_table[1]();

    printf("\n--- hook 的实现方式 ---\n");
    printf("1. 修改 sys_call_table:\n");
    printf("   write_cr0(read_cr0() & ~WP)\n");
    printf("   sys_call_table[NR] = hook_fn\n");
    printf("   write_cr0(read_cr0() | WP)\n\n");
    printf("2. inline hook:\n");
    printf("   在函数入口写入 jmp\n");
    printf("   5 字节相对跳转\n\n");
    printf("3. ftrace hook:\n");
    printf("   register_ftrace_function()\n");
    printf("   利用 mcount 机制\n\n");

    printf("--- 检测 hook ---\n");
    printf("1. 检查 sys_call_table 完整性\n");
    printf("2. 检查函数入口指令\n");
    printf("3. 比较 /proc/kallsyms\n");

    return 0;
}
```

*原来"系统调用"是"入口"的"钥匙"。*

---

## 二

林小源在研究系统调用劫持的过程中，理解了它的实现原理。

修改 sys_call_table 需要解除写保护——x86 的 CR0 寄存器有 WP (Write Protect) 位。清除 WP 位后，内核代码段也可以写入。

*原来"CR0"是"写保护"的"开关"。*

---

## 三

林小源在思考防御的过程中，理解了系统调用表保护的重要性。

现代内核通过多种手段保护 sys_call_table：KASLR 让攻击者找不到表的位置，SMEP/SMAP 防止执行用户空间代码，内核写保护防止修改代码段。

*原来"保护"是"多层"的"防线"。*

---

### 道藏笔记

::: tip 内核启示
**系统调用劫持是 rootkit 的核心技术。**

劫持方法：
- 修改 sys_call_table — 直接修改函数指针
- inline hook — 注入跳转指令
- ftrace hook — 利用 ftrace 框架
- kprobe hook — 利用 kprobe 框架

防御手段：
- KASLR — 隐藏表位置
- SMEP/SMAP — 防止执行用户代码
- 内核写保护 — 防止修改代码段

*系统调用是入口——守住入口，就守住了内核。*
:::

---

<ChapterGate
  id="syscall-hook"
  title="系统调用劫持之试"
  prompt="系统调用劫持有哪些方法？如何防御？"
  answer="方法：修改 sys_call_table、inline hook、ftrace hook、kprobe hook。防御：KASLR、SMEP/SMAP、内核写保护"
  placeholder="描述系统调用劫持"
/>

<ChapterNav prev="/novel/vol8-security/ch156" next="/novel/vol8-security/ch158" />
