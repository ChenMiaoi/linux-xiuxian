---
title: 第四十八章：调度类
---

# 第四十八章：调度类

<CultivationRealm realm="结丹后期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/sched/core.c" line="pick_next_task" />

## 一

林小源在研究调度器的过程中，理解了"调度类"的设计。

调度类是一种多态设计——每种调度类都实现了相同的接口，但有不同的调度策略。调度器按优先级依次检查每个调度类，选择第一个有可运行进程的调度类。

```c:playground:调度类 — 多态设计
#include <stdio.h>
#include <string.h>

/*
 * Linux 调度类的层次（从高到低）：
 *
 * stop_sched_class     — 最高优先级
 * dl_sched_class       — Deadline 调度器
 * rt_sched_class       — 实时调度器
 * fair_sched_class     — CFS 调度器
 * idle_sched_class     — 最低优先级
 *
 * 每个调度类实现相同的接口：
 *   enqueue_task()     — 把进程加入运行队列
 *   dequeue_task()     — 把进程从运行队列移除
 *   pick_next_task()   — 选择下一个运行的进程
 *   task_tick()        — 时钟中断处理
 *   task_fork()        — fork 时的处理
 *
 * 调度器的 pick_next_task()：
 *   按优先级检查每个调度类
 *   返回第一个有可运行进程的调度类选择的进程
 */

struct sched_class {
    const char *name;
    int priority;
    int (*pick_next)(void);
};

int pick_next_stop(void) { return -1; }   /* 通常没有进程 */
int pick_next_dl(void) { return 300; }    /* Deadline 进程 */
int pick_next_rt(void) { return 200; }    /* RT 进程 */
int pick_next_fair(void) { return 100; }  /* CFS 进程 */
int pick_next_idle(void) { return 0; }    /* idle 进程 */

int main() {
    printf("=== 调度类 — 多态设计 ===\n\n");

    struct sched_class classes[] = {
        { "stop",  0, pick_next_stop },
        { "dl",    1, pick_next_dl },
        { "rt",    2, pick_next_rt },
        { "fair",  3, pick_next_fair },
        { "idle",  4, pick_next_idle },
    };
    int nr = sizeof(classes) / sizeof(classes[0]);

    printf("调度类层次：\n");
    for (int i = 0; i < nr; i++) {
        printf("  %s (优先级 %d)\n", classes[i].name, classes[i].priority);
    }

    printf("\n--- pick_next_task() 的逻辑 ---\n");
    printf("按优先级检查每个调度类：\n\n");

    for (int i = 0; i < nr; i++) {
        int pid = classes[i].pick_next();
        printf("检查 %s: ", classes[i].name);
        if (pid > 0) {
            printf("选择 PID %d\n", pid);
            printf("→ 调度完成，返回 PID %d\n", pid);
            break;
        } else {
            printf("没有可运行进程，继续检查\n");
        }
    }

    printf("\n--- 调度类的设计哲学 ---\n");
    printf("stop:   不可抢占，用于 CPU hotplug\n");
    printf("dl:     时间保证，硬实时任务\n");
    printf("rt:     确定性，软实时任务\n");
    printf("fair:   公平，普通进程\n");
    printf("idle:   空闲，没有其他进程时运行\n");

    return 0;
}
```

*原来调度器是"多态"的。*

---

## 二

林小源在研究调度类的过程中，理解了这种设计的好处。

调度类的多态设计让内核可以轻松添加新的调度策略，而不需要修改核心调度代码。每种调度类只需要实现相同的接口，调度器按优先级依次检查。

*原来"抽象"是内核设计的核心思想。*

林小源还发现了一个细节：`stop_sched_class` 是最高优先级的调度类，它只用于 CPU hotplug 等特殊情况。`stop` 进程可以抢占任何其他进程——包括 Deadline 和 RT。

*原来"最高优先级"是为"特殊场景"准备的。*

---

## 三

林小源在观察调度类的过程中，理解了 `pick_next_task()` 的效率。

`pick_next_task()` 不需要遍历所有进程——它只需要检查每个调度类的第一个可运行进程。由于调度类的数量很少（只有 5 个），这个操作是 O(1) 的。

*原来"层次"可以提高"效率"。*

---

### 道藏笔记

::: tip 内核启示
**调度类是 Linux 调度器的多态设计。**

调度类的层次（从高到低）：
1. `stop_sched_class` — 最高优先级，用于 CPU hotplug
2. `dl_sched_class` — Deadline 调度器
3. `rt_sched_class` — 实时调度器
4. `fair_sched_class` — CFS 调度器
5. `idle_sched_class` — 最低优先级

每个调度类实现相同的接口：
- `enqueue_task()` — 把进程加入运行队列
- `dequeue_task()` — 把进程从运行队列移除
- `pick_next_task()` — 选择下一个运行的进程
- `task_tick()` — 时钟中断处理

`pick_next_task()` 的逻辑：
1. 按优先级检查每个调度类
2. 返回第一个有可运行进程的调度类选择的进程
3. 时间复杂度 O(1)

*调度类让调度器可以"扩展"——添加新策略不需要修改核心代码。*
:::

---

<ChapterGate
  id="sched-class-pick"
  title="调度类之试"
  prompt="Linux 有哪几种调度类？pick_next_task() 如何选择进程？"
  answer="stop、dl、rt、fair、idle 五种调度类。pick_next_task() 按优先级检查每个调度类，返回第一个有可运行进程的调度类选择的进程"
  placeholder="描述调度类的层次"
/>

<ChapterNav prev="/novel/vol3-scheduler/ch47" next="/novel/vol3-scheduler/ch49" />
