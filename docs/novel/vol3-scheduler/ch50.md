---
title: 第五十章：idle 之道
---

# 第五十章：idle 之道

<CultivationRealm realm="结丹后期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/sched/idle_task.c" line="pick_next_task_idle" />

## 一

林小源在调度竞技场中，终于理解了自己的位置。

idle 进程——PID 0——是调度器的"最后选择"。当没有任何其他进程可运行时，调度器才会选择 idle 进程。

```c:playground:idle 进程 — 最后的选择
#include <stdio.h>

/*
 * idle 进程的特殊性：
 * 1. 是调度器的"最后选择"
 * 2. 当没有其他进程可运行时运行
 * 3. 执行 HLT/WFI 指令，让 CPU 进入低功耗状态
 * 4. 每个 CPU 都有自己的 idle 进程
 *
 * idle 的调度类：idle_sched_class
 *   pick_next_task_idle() 总是返回 idle 进程
 *   只有当所有其他调度类都没有可运行进程时才被选中
 *
 * idle 的工作：
 *   1. 执行 HLT/WFI 指令（CPU 低功耗）
 *   2. 等待中断唤醒
 *   3. 中断发生 → 检查是否有进程可运行
 *   4. 有 → 切换到该进程
 *   5. 没有 → 继续 HLT/WFI
 */

struct sched_class {
    const char *name;
    int has_runnable;
};

int main() {
    printf("=== idle 进程 — 最后的选择 ===\n\n");

    /* 模拟调度器的选择过程 */
    struct sched_class classes[] = {
        { "stop",   0 },  /* 通常没有进程 */
        { "dl",     0 },  /* 通常没有进程 */
        { "rt",     1 },  /* 有 RT 进程 */
        { "fair",   1 },  /* 有 CFS 进程 */
        { "idle",   1 },  /* 总是有 idle */
    };
    int nr = sizeof(classes) / sizeof(classes[0]);

    printf("场景 1: 有 RT 和 CFS 进程可运行\n");
    for (int i = 0; i < nr; i++) {
        if (classes[i].has_runnable) {
            printf("  选择: %s\n", classes[i].name);
            printf("  → idle 不会被选中\n\n");
            break;
        }
    }

    /* 模拟所有进程都睡眠的场景 */
    classes[2].has_runnable = 0;  /* RT 进程睡眠 */
    classes[3].has_runnable = 0;  /* CFS 进程睡眠 */

    printf("场景 2: 所有进程都睡眠\n");
    for (int i = 0; i < nr; i++) {
        if (classes[i].has_runnable) {
            printf("  选择: %s\n", classes[i].name);
            printf("  → idle 被选中，CPU 进入低功耗\n\n");
            break;
        }
    }

    printf("--- idle 的工作 ---\n");
    printf("1. 执行 HLT/WFI 指令\n");
    printf("   CPU 进入低功耗状态\n");
    printf("   等待中断唤醒\n\n");
    printf("2. 中断发生\n");
    printf("   CPU 醒来\n");
    printf("   检查是否有进程可运行\n\n");
    printf("3. 有进程可运行\n");
    printf("   切换到该进程\n");
    printf("   idle 继续等待\n\n");
    printf("4. 没有进程可运行\n");
    printf("   继续 HLT/WFI\n");
    printf("   等待下一个中断\n");

    printf("\n--- idle 的意义 ---\n");
    printf("idle 不是\"浪费\"CPU 时间\n");
    printf("idle 是\"节省\"电能\n");
    printf("idle 让 CPU 在空闲时进入低功耗状态\n");
    printf("idle 是\"绿色计算\"的基础\n");

    return 0;
}
```

*原来 idle 不是"无用"——是"节能"。*

---

## 二

林小源在研究 idle 的过程中，理解了自己在调度器中的位置。

idle 进程是调度器的"底线"——当没有任何其他进程可运行时，idle 保证 CPU 不会空转。它执行 `HLT`（x86）或 `WFI`（RISC-V）指令，让 CPU 进入低功耗状态，等待中断唤醒。

*原来"等待"也是一种"工作"。*

林小源想起了自己的身份——被 `#ifdef 0` 封印的代码。他不是普通的 idle——他试图在空闲时做"有意义"的事情。但这个想法被认为"没有价值"，所以被封印了。

*也许"无用"和"有用"之间的界限，没有那么清晰。*

---

## 三

林小源在观察 idle 的过程中，理解了一个哲学问题。

idle 进程是"最后的选择"——它只在没有其他进程时运行。这意味着，idle 的存在价值在于"不被选择"。如果 idle 被频繁选择，说明系统很空闲；如果 idle 很少被选择，说明系统很忙碌。

*原来"存在"的意义可以是"不被需要"。*

---

### 道藏笔记

::: tip 内核启示
**idle 进程是调度器的"最后选择"。**

idle 的特殊性：
- `idle_sched_class` 是最低优先级的调度类
- 只有当所有其他调度类都没有可运行进程时才被选中
- 每个 CPU 都有自己的 idle 进程

idle 的工作：
1. 执行 `HLT`/`WFI` 指令，CPU 进入低功耗状态
2. 等待中断唤醒
3. 中断发生 → 检查是否有进程可运行
4. 有 → 切换到该进程
5. 没有 → 继续 `HLT`/`WFI`

idle 的意义：
- 不是"浪费"CPU 时间
- 而是"节省"电能
- 让 CPU 在空闲时进入低功耗状态
- 是"绿色计算"的基础

*idle 是"最后的选择"——但没有它，系统无法运转。*
:::

---

<ChapterGate
  id="idle-hlt"
  title="idle 之试"
  prompt="idle 进程什么时候运行？它执行什么指令？"
  answer="当所有其他进程都睡眠时，idle 进程运行。它执行 HLT/WFI 指令让 CPU 进入低功耗状态"
  placeholder="描述 idle 进程的作用"
/>

<ChapterNav prev="/novel/vol3-scheduler/ch49" next="/novel/vol3-scheduler/ch51" />
