---
title: 第三十八章：红黑树
---

# 第三十八章：红黑树

<CultivationRealm realm="结丹初期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/sched/fair.c" line="__enqueue_entity" />

## 一

林小源在调度仙子的指引下，开始研究红黑树。

红黑树是 CFS 的核心数据结构——所有可运行的进程都以 `sched_entity` 的形式挂在红黑树上。树的最左边是 `vruntime` 最小的进程——它就是下一个获得 CPU 的进程。

```c:playground:红黑树 — 调度的基石
#include <stdio.h>
#include <stdlib.h>

/*
 * 红黑树的性质：
 * 1. 每个节点是红色或黑色
 * 2. 根节点是黑色
 * 3. 所有叶子节点（NIL）是黑色
 * 4. 红色节点的两个子节点都是黑色
 * 5. 从根到叶子的每条路径上，黑色节点数相同
 *
 * 红黑树保证了：
 * - 最长路径 ≤ 2 × 最短路径
 * - 查找、插入、删除都是 O(log n)
 *
 * CFS 使用红黑树：
 * - 按 vruntime 排序
 * - 最左节点 = vruntime 最小 = 下一个运行的进程
 * - 缓存最左节点，O(1) 获取
 */

struct rb_node {
    unsigned long __rb_parent_color;
    struct rb_node *rb_right;
    struct rb_node *rb_left;
};

struct sched_entity {
    struct rb_node run_node;
    unsigned long vruntime;
    int pid;
    char comm[16];
};

/* 简化的红黑树（用数组模拟中序遍历） */
struct rb_tree {
    int values[16];
    int count;
};

void rb_insert(struct rb_tree *tree, int value) {
    int i = tree->count;
    /* 简化：直接插入并排序 */
    tree->values[i] = value;
    tree->count++;
    /* 插入排序 */
    for (int j = i; j > 0 && tree->values[j] < tree->values[j-1]; j--) {
        int tmp = tree->values[j];
        tree->values[j] = tree->values[j-1];
        tree->values[j-1] = tmp;
    }
}

int main() {
    printf("=== 红黑树 — 调度的基石 ===\n\n");

    /* 模拟几个进程的 vruntime */
    struct rb_tree tree = { .count = 0 };

    int vruntimes[] = { 1200, 800, 1500, 600, 1000, 900 };
    const char *names[] = { "vim", "shell", "compiler", "cron", "ssh", "logger" };
    int nr = sizeof(vruntimes) / sizeof(vruntimes[0]);

    printf("插入进程到红黑树（按 vruntime 排序）：\n");
    for (int i = 0; i < nr; i++) {
        rb_insert(&tree, vruntimes[i]);
        printf("  插入 %s (vruntime=%d)\n", names[i], vruntimes[i]);
    }

    printf("\n红黑树中序遍历（按 vruntime 排序）：\n");
    for (int i = 0; i < tree.count; i++) {
        printf("  vruntime = %d", tree.values[i]);
        if (i == 0) printf("  ← 最左节点（下一个运行）");
        printf("\n");
    }

    printf("\n--- 红黑树的特性 ---\n");
    printf("查找最左节点: O(1) — 缓存在 rb_leftmost\n");
    printf("插入新节点:   O(log n)\n");
    printf("删除节点:     O(log n)\n");
    printf("更新 vruntime: O(log n) — 需要重新排序\n");

    printf("\n--- 红黑树 vs 其他数据结构 ---\n");
    printf("链表:   插入 O(1)，查找最左 O(n)\n");
    printf("堆:     插入 O(log n)，查找最左 O(1)\n");
    printf("红黑树: 插入 O(log n)，查找最左 O(1)\n");
    printf("        额外支持范围查询、前驱后继\n");

    return 0;
}
```

*原来红黑树是调度仙子的"天秤"。*

---

## 二

林小源在研究红黑树的过程中，理解了 CFS 选择红黑树的原因。

红黑树的有序性保证了最左节点总是 `vruntime` 最小的进程——这正是 CFS 需要的。而且红黑树的平衡性保证了插入和删除的时间复杂度是 O(log n)——即使有数千个可运行的进程，调度器也能高效地做出决策。

*原来数据结构的选择，决定了算法的效率。*

林小源还发现了一个优化：内核缓存了红黑树的最左节点。这样 `pick_next_task()` 不需要遍历红黑树——直接返回最左节点即可，O(1) 时间复杂度。

*原来"缓存"是内核优化的常用手段。*

---

## 三

林小源在观察红黑树的过程中，注意到了一个有趣的细节。

当一个进程的 `vruntime` 更新时，它需要在红黑树中重新定位。这意味着先删除旧节点，再插入新节点——两次 O(log n) 操作。

*原来"更新"比"插入"更昂贵。*

但 CFS 有一个优化：如果进程只是继续运行（没有被抢占），它的 `vruntime` 更新不需要立即反映到红黑树中。只有当进程被抢占或主动让出 CPU 时，才需要更新红黑树。

*原来"延迟更新"也是一种优化策略。*

---

### 道藏笔记

::: tip 内核启示
**红黑树是 CFS 的核心数据结构。**

红黑树的性质：
- 自平衡二叉搜索树
- 最长路径 ≤ 2 × 最短路径
- 查找、插入、删除都是 O(log n)

CFS 中的红黑树：
- 按 `vruntime` 排序
- 最左节点 = `vruntime` 最小的进程
- 缓存最左节点，`pick_next_task()` 是 O(1)

红黑树的操作：
- `__enqueue_entity()` — 插入调度实体
- `__dequeue_entity()` — 删除调度实体
- `__pick_first_entity()` — 获取最左节点

为什么选择红黑树：
- 有序性：最左节点总是 `vruntime` 最小
- 平衡性：O(log n) 的插入和删除
- 额外功能：范围查询、前驱后继

*红黑树是调度仙子的天秤——永远平衡，永远公平。*
:::

---

<ChapterGate
  id="rbtree-leftmost"
  title="红黑树之试"
  prompt="CFS 为什么使用红黑树？最左节点代表什么？"
  answer="红黑树保证 O(log n) 的插入删除和 O(1) 的最左节点查找。最左节点是 vruntime 最小的进程，即下一个运行的进程"
  placeholder="描述红黑树在 CFS 中的作用"
/>

<ChapterNav prev="/novel/vol3-scheduler/ch37" next="/novel/vol3-scheduler/ch39" />
