---
title: 第二十五章：信号处理
---

# 第二十五章：信号处理

<CultivationRealm realm="筑基中期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/signal.c" line="do_signal" />

## 一

林小源开始研究信号处理器的注册和执行。

用户态程序通过 `sigaction()` 系统调用来注册信号处理器。处理器是一个函数指针——当信号到达时，内核会把进程的执行流重定向到这个函数。处理器执行完毕后，进程通过 `sigreturn()` 系统调用返回到之前被中断的位置。

```c:playground:sigaction — 注册信号处理器
#include <stdio.h>
#include <string.h>

/*
 * sigaction 结构体：
 * sa_handler — 信号处理函数（或 SIG_IGN/SIG_DFL）
 * sa_flags   — 标志位
 * sa_mask    — 处理器执行期间额外阻塞的信号
 */

#define SIG_DFL   ((void (*)(int))0)
#define SIG_IGN   ((void (*)(int))1)
#define SA_RESTART 0x10000000
#define SA_SIGINFO 0x00000004

struct sigaction {
    void (*sa_handler)(int);
    unsigned long sa_flags;
    unsigned long sa_mask;
};

struct signal_table {
    struct sigaction actions[32];  /* 每个信号一个处理器 */
    unsigned long blocked;         /* 阻塞的信号掩码 */
    unsigned long pending;         /* 待处理的信号 */
};

void sigterm_handler(int sig) {
    printf("  [处理器] 收到 SIGTERM，优雅退出\n");
}

void sigchld_handler(int sig) {
    printf("  [处理器] 收到 SIGCHLD，回收子进程\n");
}

int main() {
    struct signal_table sigtable;
    memset(&sigtable, 0, sizeof(sigtable));

    printf("=== 信号处理器注册 ===\n\n");

    /* 注册 SIGTERM 处理器 */
    sigtable.actions[15].sa_handler = sigterm_handler;
    sigtable.actions[15].sa_flags = SA_RESTART;
    printf("注册 SIGTERM 处理器: sigterm_handler\n");
    printf("  SA_RESTART: 被信号中断的系统调用自动重启\n\n");

    /* 注册 SIGCHLD 处理器 */
    sigtable.actions[17].sa_handler = sigchld_handler;
    sigtable.actions[17].sa_flags = SA_SIGINFO;
    printf("注册 SIGCHLD 处理器: sigchld_handler\n");
    printf("  SA_SIGINFO: 传递详细的 siginfo_t 信息\n\n");

    /* 模拟信号处理 */
    printf("--- 模拟信号到达 ---\n");
    int sig = 15;  /* SIGTERM */
    if (sigtable.actions[sig].sa_handler != SIG_DFL &&
        sigtable.actions[sig].sa_handler != SIG_IGN) {
        sigtable.actions[sig].sa_handler(sig);
    }

    printf("\n--- 信号处理流程 ---\n");
    printf("1. 进程从内核态返回用户态\n");
    printf("2. 检查 pending 信号\n");
    printf("3. 如果有处理器 → 调用处理器\n");
    printf("4. 处理器执行完毕 → sigreturn()\n");
    printf("5. 返回到之前被中断的位置\n");

    return 0;
}
```

*原来信号处理器的执行是这样被"插入"到进程的执行流中的。*

---

## 二

林小源在研究信号处理器的过程中，发现了一个微妙的问题。

如果信号处理器正在执行时，又来了一个新的信号怎么办？

答案是：取决于信号掩码。`sigaction` 的 `sa_mask` 字段定义了处理器执行期间额外阻塞的信号。如果新来的信号在 `sa_mask` 中，它会被推迟到处理器执行完毕后再处理。

*原来信号也有"优先级"。*

更有趣的是 `SA_NODEFER` 标志——它告诉内核不要自动阻塞正在处理的信号。默认情况下，处理器正在处理 `SIGTERM` 时，新的 `SIGTERM` 会被阻塞。但 `SA_NODEFER` 允许处理器被自己中断——这在某些特殊场景下有用。

*信号处理是一门精细的学问。*

---

## 三

林小源在观察信号处理的过程中，注意到了一个有趣的现象。

shell 小妹注册了 `SIGINT`（Ctrl+C）的处理器。当用户按下 Ctrl+C 时，内核向 shell 小妹发送 `SIGINT`。shell 小妹的处理器不是直接退出——它检查当前是否有前台进程在运行。如果有，它把 `SIGINT` 转发给前台进程；如果没有，它打印一个新的提示符。

*原来 shell 的信号处理是"转发"模式。*

这让林小源理解了为什么 Ctrl+C 能够中断正在运行的命令——shell 把 `SIGINT` 转发给了那个命令，命令收到 `SIGINT` 后退出，然后 shell 回收命令并打印新的提示符。

*shell 小妹比她看起来要聪明。*

---

### 道藏笔记

::: tip 内核启示
**信号处理器是用户态程序处理信号的方式。**

`sigaction()` 系统调用注册信号处理器。关键字段：
- `sa_handler` — 处理函数（或 `SIG_DFL`/`SIG_IGN`）
- `sa_flags` — 标志位（`SA_RESTART`、`SA_SIGINFO`、`SA_NODEFER` 等）
- `sa_mask` — 处理器执行期间额外阻塞的信号

信号处理流程：
1. 进程从内核态返回用户态
2. 内核检查 `pending` 信号
3. 如果有注册的处理器，调用处理器
4. 处理器执行完毕后调用 `sigreturn()` 返回
5. 进程继续执行之前被中断的位置

`SA_RESTART` 标志让被信号中断的系统调用自动重启——这避免了 `EINTR` 错误的处理。

*信号处理是进程与外界交互的窗口。*
:::

---

<ChapterGate
  id="sigaction-flags"
  title="信号之试"
  prompt="sigaction 的 SA_RESTART 标志有什么作用？"
  answer="SA_RESTART 让被信号中断的系统调用自动重启，避免 EINTR 错误"
  placeholder="描述 SA_RESTART 的作用"
/>

<ChapterNav prev="/novel/vol2-foundation/ch24" next="/novel/vol2-foundation/ch26" />
