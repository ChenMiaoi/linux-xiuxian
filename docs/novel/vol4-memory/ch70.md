---
title: 第七十章：vmalloc
---

# 第七十章：vmalloc

<CultivationRealm realm="元婴后期" />

> **涉及内核源码：** <KernelSourceLink path="mm/vmalloc.c" line="vmalloc" />

## 一

林小源在研究内核内存分配的过程中，发现了一种特殊的分配方式：`vmalloc()`。

`kmalloc()` 分配的内存是物理连续的——但当需要分配大块内存时，可能找不到连续的物理页面。`vmalloc()` 分配的内存是虚拟连续的——物理页面可以不连续，但通过页表映射成连续的虚拟地址。

```c:playground:vmalloc — 虚拟连续的内存
#include <stdio.h>

/*
 * kmalloc vs vmalloc:
 *
 * kmalloc:
 *   - 物理连续
 *   - 虚拟连续
 *   - 适合小块内存（通常 < 128KB）
 *   - 使用 Slab 分配器
 *   - 快速
 *
 * vmalloc:
 *   - 物理不连续
 *   - 虚拟连续
 *   - 适合大块内存（可以分配几 MB）
 *   - 使用页表映射
 *   - 较慢（需要修改页表）
 *
 * vmalloc 的工作原理：
 *   1. 在内核虚拟地址空间中找到一段连续的区域
 *   2. 逐页分配物理页面（不连续）
 *   3. 修改页表，把虚拟页面映射到物理页面
 *   4. 返回虚拟地址
 */

struct vm_struct {
    void *addr;         /* 虚拟地址 */
    unsigned long size; /* 大小 */
    int nr_pages;       /* 物理页数 */
    unsigned long pages[4];  /* 物理页地址（不连续） */
};

int main() {
    printf("=== vmalloc — 虚拟连续的内存 ===\n\n");

    /* 模拟 vmalloc 分配 16KB (4 页) */
    struct vm_struct vm = {
        .addr = (void *)0xFFFF800010000000,
        .size = 16384,
        .nr_pages = 4,
    };

    /* 物理页面不连续 */
    vm.pages[0] = 0x10000000;  /* 物理地址 1 */
    vm.pages[1] = 0x20003000;  /* 物理地址 2 */
    vm.pages[2] = 0x30001000;  /* 物理地址 3 */
    vm.pages[3] = 0x0FFF8000;  /* 物理地址 4 */

    printf("vmalloc 分配 16KB:\n");
    printf("  虚拟地址: %p\n", vm.addr);
    printf("  大小: %lu 字节\n", vm.size);
    printf("  物理页数: %d\n\n", vm.nr_pages);

    printf("物理页面（不连续）：\n");
    for (int i = 0; i < vm.nr_pages; i++) {
        unsigned long va = (unsigned long)vm.addr + i * 4096;
        printf("  VA 0x%lx → PA 0x%lx\n", va, vm.pages[i]);
    }

    printf("\n--- kmalloc vs vmalloc ---\n");
    printf("kmalloc:\n");
    printf("  物理连续: 是\n");
    printf("  虚拟连续: 是\n");
    printf("  大小限制: 通常 < 128KB\n");
    printf("  速度: 快\n");
    printf("  用途: 设备驱动、小数据结构\n\n");
    printf("vmalloc:\n");
    printf("  物理连续: 否\n");
    printf("  虚拟连续: 是\n");
    printf("  大大限制: 可以分配几 MB\n");
    printf("  速度: 较慢\n");
    printf("  用途: 大缓冲区、模块加载\n\n");

    printf("--- vmalloc 的使用场景 ---\n");
    printf("1. 分配大缓冲区（如网络缓冲区）\n");
    printf("2. 加载内核模块\n");
    printf("3. 分配大的数据结构\n");
    printf("4. 不需要物理连续的场景\n");

    return 0;
}
```

*原来"虚拟连续"可以掩盖"物理不连续"。*

---

## 二

林小源在研究 `vmalloc` 的过程中，理解了内核虚拟地址空间的布局。

内核的虚拟地址空间被划分成几个区域：
- 直接映射区 — 物理内存直接映射到虚拟地址（`kmalloc` 使用）
- vmalloc 区 — `vmalloc` 使用的虚拟地址空间
- 固定映射区 — 特殊用途的固定映射

*原来内核的地址空间也有"分区"。*

---

## 三

林小源在观察 `vmalloc` 的过程中，理解了它的代价。

`vmalloc` 需要修改页表——每次分配都需要逐页映射。这比 `kmalloc` 慢得多。而且 `vmalloc` 分配的内存不能用于 DMA——因为 DMA 需要物理连续的内存。

*原来"虚拟连续"有"代价"。*

---

### 道藏笔记

::: tip 内核启示
**`vmalloc()` 分配虚拟连续但物理不连续的内存。**

`vmalloc` 的工作原理：
1. 在内核虚拟地址空间中找到连续区域
2. 逐页分配物理页面（不连续）
3. 修改页表，映射虚拟页面到物理页面
4. 返回虚拟地址

`kmalloc` vs `vmalloc`：
- `kmalloc` — 物理连续，快速，适合小块
- `vmalloc` — 物理不连续，较慢，适合大块

`vmalloc` 的使用场景：
1. 大缓冲区
2. 内核模块加载
3. 大数据结构
4. 不需要物理连续的场景

`vmalloc` 的代价：
- 需要修改页表
- 不能用于 DMA
- TLB 压力更大

*`vmalloc` 是"虚拟连续"的魔法——物理不连续，但看起来连续。*
:::

---

<ChapterGate
  id="vmalloc-kmalloc"
  title="vmalloc 之试"
  prompt="vmalloc 和 kmalloc 有什么区别？为什么 vmalloc 不能用于 DMA？"
  answer="kmalloc 分配物理连续内存，vmalloc 分配虚拟连续但物理不连续内存。DMA 需要物理连续的内存，所以 vmalloc 不适用"
  placeholder="描述 vmalloc 的特点"
/>

<ChapterNav prev="/novel/vol4-memory/ch69" next="/novel/vol4-memory/ch71" />
