---
title: 第五十六章：内景初开
---

# 第五十六章：内景初开

<CultivationRealm realm="元婴初期" />

> **涉及内核源码：** <KernelSourceLink path="include/linux/mm_types.h" line="struct mm_struct" />

## 一

林小源踏入了内存管理的世界。

这是一个全新的领域——比进程管理更复杂，比调度器更危险。在这里，虚拟与现实交织，每一个地址都是一场幻觉，每一次访问都可能触发页 fault。

*内景天地，自成世界。*

```c:playground:mm_struct — 进程的内存世界
#include <stdio.h>
#include <stddef.h>

/*
 * mm_struct 是进程地址空间的描述符。
 * 它包含了进程的所有内存信息：
 *   - VMA 列表（虚拟内存区域）
 *   - 页全局目录（PGD）
 *   - 各种统计信息
 *
 * 关键字段：
 *   pgd       — 页全局目录（顶级页表）
 *   mmap      — VMA 链表头
 *   mm_count  — 引用计数
 *   map_count — VMA 数量
 *   total_vm  — 总虚拟内存页数
 *   locked_vm — 锁定的页数
 *   data_vm   — 数据段页数
 *   start_code, end_code — 代码段范围
 *   start_data, end_data — 数据段范围
 *   start_brk, brk       — 堆的范围
 *   start_stack          — 栈的起始地址
 */

struct mm_struct {
    void *pgd;              /* 页全局目录 */
    void *mmap;             /* VMA 链表 */
    int mm_count;           /* 引用计数 */
    int map_count;          /* VMA 数量 */
    unsigned long total_vm; /* 总虚拟页数 */
    unsigned long locked_vm;
    unsigned long data_vm;
    unsigned long start_code, end_code;
    unsigned long start_data, end_data;
    unsigned long start_brk, brk;
    unsigned long start_stack;
};

int main() {
    printf("=== mm_struct — 进程的内存世界 ===\n\n");

    /* 模拟一个进程的地址空间 */
    struct mm_struct mm = {
        .pgd = (void *)0xFFFF800000001000,
        .mmap = NULL,
        .mm_count = 1,
        .map_count = 5,
        .total_vm = 1024,
        .locked_vm = 0,
        .data_vm = 256,
        .start_code = 0x00400000,
        .end_code   = 0x00401000,
        .start_data = 0x00600000,
        .end_data   = 0x00601000,
        .start_brk  = 0x00800000,
        .brk        = 0x00810000,
        .start_stack = 0x7FFF0000,
    };

    printf("进程地址空间布局：\n");
    printf("  代码段: 0x%lx - 0x%lx\n", mm.start_code, mm.end_code);
    printf("  数据段: 0x%lx - 0x%lx\n", mm.start_data, mm.end_data);
    printf("  堆:     0x%lx - 0x%lx\n", mm.start_brk, mm.brk);
    printf("  栈:     0x%lx\n\n", mm.start_stack);

    printf("内存统计：\n");
    printf("  总虚拟页数: %lu\n", mm.total_vm);
    printf("  VMA 数量:   %d\n", mm.map_count);
    printf("  引用计数:   %d\n\n", mm.mm_count);

    printf("--- 进程地址空间的组成 ---\n");
    printf("  代码段 (.text):   只读，可执行\n");
    printf("  数据段 (.data):   可读写\n");
    printf("  BSS 段 (.bss):    未初始化数据，全零\n");
    printf("  堆 (heap):        动态分配，向高地址增长\n");
    printf("  栈 (stack):       函数调用帧，向低地址增长\n");
    printf("  内存映射 (mmap):  文件映射、匿名映射\n");

    printf("\n--- mm_struct 的角色 ---\n");
    printf("  是进程地址空间的\"总管\"\n");
    printf("  管理所有 VMA（虚拟内存区域）\n");
    printf("  指向页全局目录（PGD）\n");
    printf("  记录内存使用统计\n");

    return 0;
}
```

*原来进程的"内心世界"如此广阔。*

---

## 二

林小源在研究 `mm_struct` 的过程中，理解了虚拟内存的本质。

每个进程都以为自己拥有整个地址空间——从 0 到 2^64（在 64 位系统中）。但这是幻觉。进程看到的是虚拟地址，实际的物理地址由页表转换。

*原来"虚拟"是内核给进程的"幻术"。*

这种幻术的好处是：每个进程都有独立的地址空间，互不干扰。进程 A 不能访问进程 B 的内存——除非它们共享（如 `CLONE_VM`）。

*原来"隔离"是虚拟内存的核心价值。*

---

## 三

林小源在观察 `mm_struct` 的过程中，注意到了一个细节。

内核线程的 `mm` 为 `NULL`。这是因为内核线程没有用户态地址空间——它们只在内核态运行，使用内核的页表。

*原来"没有 mm"是内核线程的"标志"。*

---

### 道藏笔记

::: tip 内核启示
**`mm_struct` 是进程地址空间的描述符。**

关键字段：
- `pgd` — 页全局目录（顶级页表）
- `mmap` — VMA 链表
- `map_count` — VMA 数量
- `total_vm` — 总虚拟内存页数
- `start_code/end_code` — 代码段范围
- `start_brk/brk` — 堆的范围
- `start_stack` — 栈的起始地址

进程地址空间布局：
- 代码段（.text）— 只读，可执行
- 数据段（.data）— 可读写
- BSS 段（.bss）— 未初始化数据
- 堆（heap）— 动态分配
- 栈（stack）— 函数调用帧
- 内存映射（mmap）— 文件映射、匿名映射

内核线程的 `mm` 为 `NULL` — 没有用户态地址空间。

*虚拟内存是内核给进程的"幻术"——每个进程都以为自己拥有整个世界。*
:::

---

<ChapterGate
  id="mm-struct"
  title="内景初开之试"
  prompt="mm_struct 的作用是什么？进程地址空间由哪些部分组成？"
  answer="mm_struct 是进程地址空间的描述符，管理 VMA 和页表。地址空间由代码段、数据段、BSS、堆、栈、mmap 组成"
  placeholder="描述 mm_struct 的作用"
/>

<ChapterNav prev="/novel/vol3-scheduler/ch55" next="/novel/vol4-memory/ch57" />
