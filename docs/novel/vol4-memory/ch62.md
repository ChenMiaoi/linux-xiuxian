---
title: 第六十二章：写时复制
---

# 第六十二章：写时复制

<CultivationRealm realm="元婴中期" />

> **涉及内核源码：** <KernelSourceLink path="mm/memory.c" line="wp_page_copy" />

## 一

林小源在研究 `fork()` 的过程中，再次遇到了写时复制（COW）。

在第二卷中，他了解了 COW 的基本概念。现在，在内存管理的世界中，他终于看到了 COW 的真正实现。

```c:playground:COW — 写时复制的深层实现
#include <stdio.h>

/*
 * 写时复制（Copy-on-Write，COW）的实现：
 *
 * fork() 时：
 *   1. 子进程复制父进程的页表
 *   2. 所有页面标记为只读 + COW
 *   3. 页面的引用计数增加
 *
 * 写入时：
 *   1. CPU 尝试写入只读页面
 *   2. 触发页 fault
 *   3. 内核检查 PTE 标志
 *   4. 如果是 COW 页面：
 *      a. 分配新的物理页
 *      b. 复制内容
 *      c. 更新页表为可写
 *      d. 减少原页面的引用计数
 *   5. 进程继续执行
 *
 * COW 的好处：
 *   - fork() 不需要复制所有页面
 *   - 只有在写入时才复制
 *   - 如果 execve() 立即调用，根本不需要复制
 */

#define PTE_V (1 << 0)
#define PTE_R (1 << 1)
#define PTE_W (1 << 2)
#define PTE_COW (1 << 8)  /* 自定义 COW 标志 */

struct page {
    int refcount;
    char data[64];  /* 简化的页面数据 */
};

struct pte {
    unsigned long flags;
    struct page *page;
};

int main() {
    printf("=== COW — 写时复制的深层实现 ===\n\n");

    /* 模拟物理页 */
    struct page phys_page = { .refcount = 1 };
    sprintf(phys_page.data, "Hello from parent");

    /* 父进程的 PTE */
    struct pte parent_pte = {
        .flags = PTE_V | PTE_R | PTE_W,
        .page = &phys_page,
    };

    printf("fork() 之前：\n");
    printf("  物理页 refcount = %d\n", phys_page.refcount);
    printf("  PTE flags: V=%d R=%d W=%d COW=%d\n\n",
           (parent_pte.flags & PTE_V) ? 1 : 0,
           (parent_pte.flags & PTE_R) ? 1 : 0,
           (parent_pte.flags & PTE_W) ? 1 : 0,
           (parent_pte.flags & PTE_COW) ? 1 : 0);

    /* fork() — 子进程复制页表，标记为 COW */
    printf("fork() 执行：\n");
    printf("  子进程复制父进程的页表\n");
    printf("  所有页面标记为只读 + COW\n\n");

    /* 更新 PTE */
    parent_pte.flags &= ~PTE_W;  /* 清除写标志 */
    parent_pte.flags |= PTE_COW; /* 设置 COW 标志 */
    phys_page.refcount = 2;       /* 引用计数增加 */

    printf("fork() 之后：\n");
    printf("  物理页 refcount = %d\n", phys_page.refcount);
    printf("  PTE flags: V=%d R=%d W=%d COW=%d\n\n",
           (parent_pte.flags & PTE_V) ? 1 : 0,
           (parent_pte.flags & PTE_R) ? 1 : 0,
           (parent_pte.flags & PTE_W) ? 1 : 0,
           (parent_pte.flags & PTE_COW) ? 1 : 0);

    /* 子进程尝试写入 → 触发 COW */
    printf("子进程尝试写入 → 触发页 fault\n");
    printf("  内核检测到 COW 标志\n");
    printf("  分配新的物理页\n");
    printf("  复制内容\n");
    printf("  更新页表为可写\n");
    printf("  减少原页面的引用计数\n\n");

    /* COW 完成 */
    struct page new_page = { .refcount = 1 };
    sprintf(new_page.data, "Hello from parent");
    phys_page.refcount = 1;

    printf("COW 完成：\n");
    printf("  原物理页 refcount = %d\n", phys_page.refcount);
    printf("  新物理页 refcount = %d\n", new_page.refcount);
    printf("  子进程现在有自己的页面\n");

    printf("\n--- COW 的优势 ---\n");
    printf("1. fork() 快速 — 只复制页表，不复制页面\n");
    printf("2. 节省内存 — 共享页面直到写入\n");
    printf("3. execve() 友好 — 如果立即 execve()，不需要复制\n");

    return 0;
}
```

*原来 COW 是"共享"和"独立"的平衡。*

---

## 二

林小源在研究 COW 的过程中，理解了页面引用计数的作用。

当多个进程共享同一个物理页面时（通过 COW），页面的引用计数大于 1。当引用计数降为 1 时，说明只有一个进程使用这个页面，可以安全地直接写入——不需要 COW。

*原来"引用计数"是"共享"的度量。*

---

## 三

林小源在观察 COW 的过程中，理解了 `fork()` + `execve()` 的优化。

`fork()` 后立即 `execve()` 是最常见的用法（如 shell 执行命令）。由于 COW，`fork()` 不需要复制任何物理页面——只复制页表。`execve()` 会替换整个地址空间，所以之前共享的页面会被释放。

*原来 COW 是为 `fork()` + `execve()` 优化的。*

---

### 道藏笔记

::: tip 内核启示
**写时复制（COW）是 `fork()` 的核心优化。**

COW 的实现：
1. `fork()` 时，子进程复制父进程的页表
2. 所有页面标记为只读 + COW
3. 页面的引用计数增加
4. 写入时触发页 fault
5. 内核分配新页面，复制内容，更新页表
6. 引用计数减少

COW 的优势：
1. `fork()` 快速 — 只复制页表
2. 节省内存 — 共享页面直到写入
3. `execve()` 友好 — 立即 `execve()` 时不需要复制

页面引用计数：
- `refcount > 1` — 多个进程共享，写入需要 COW
- `refcount == 1` — 独占，可以直接写入

*COW 是"共享"和"独立"的平衡——不写不复制，写了才分离。*
:::

---

<ChapterGate
  id="cow-refcount"
  title="写时复制之试"
  prompt="COW 在什么时候复制页面？页面引用计数有什么作用？"
  answer="COW 在写入时才复制页面（触发页 fault）。引用计数大于 1 表示多个进程共享，写入需要 COW；等于 1 表示独占"
  placeholder="描述 COW 的触发条件"
/>

<ChapterNav prev="/novel/vol4-memory/ch61" next="/novel/vol4-memory/ch63" />
