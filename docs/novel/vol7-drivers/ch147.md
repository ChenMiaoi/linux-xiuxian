---
title: 第一百四十七章：SCSI
---

# 第一百四十七章：SCSI

<CultivationRealm realm="合道期" />

> **涉及内核源码：** <KernelSourceLink path="drivers/scsi/scsi_lib.c" />

## 一

林小源在设备山脉中发现了存储设备的核心——SCSI 子系统。

SCSI（Small Computer System Interface）是 Linux 存储的通用框架——无论是 SATA、SAS 还是 USB 存储，都通过 SCSI 子系统访问。

```c:playground:SCSI — 存储的通用框架
#include <stdio.h>

/*
 * SCSI 子系统:
 *
 *   SCSI 上层: sd (磁盘), sr (光驱), st (磁带)
 *   SCSI 中层: SCSI 核心
 *   SCSI 下层: host adapter 驱动
 *
 * SCSI 命令:
 *   READ(10) — 读取数据
 *   WRITE(10) — 写入数据
 *   INQUIRY — 查询设备信息
 *   TEST UNIT READY — 测试设备就绪
 *
 * SCSI 的意义:
 *   统一的存储接口
 *   支持多种设备
 *   支持多种传输方式
 */

int main() {
    printf("=== SCSI — 存储的通用框架 ===\n\n");

    printf("SCSI 子系统:\n\n");
    printf("  SCSI 上层:\n");
    printf("    sd — 磁盘\n");
    printf("    sr — 光驱\n");
    printf("    st — 磁带\n\n");
    printf("  SCSI 中层:\n");
    printf("    SCSI 核心\n\n");
    printf("  SCSI 下层:\n");
    printf("    host adapter 驱动\n\n");

    printf("--- SCSI 命令 ---\n");
    printf("READ(10): 读取数据\n");
    printf("WRITE(10): 写入数据\n");
    printf("INQUIRY: 查询设备信息\n");
    printf("TEST UNIT READY: 测试就绪\n\n");

    printf("--- SCSI 的意义 ---\n");
    printf("统一的存储接口:\n");
    printf("  不管底层是什么\n");
    printf("  上层都看到 SCSI 设备\n\n");
    printf("支持多种设备:\n");
    printf("  SATA 硬盘\n");
    printf("  SAS 硬盘\n");
    printf("  USB 存储\n");
    printf("  NVMe (部分)\n\n");

    printf("--- /dev/sd* ---\n");
    printf("/dev/sda — 第一块 SCSI 磁盘\n");
    printf("/dev/sdb — 第二块 SCSI 磁盘\n");
    printf("/dev/sda1 — 第一块磁盘的第一个分区\n\n");

    printf("--- SCSI vs NVMe ---\n");
    printf("SCSI:\n");
    printf("  通用存储框架\n");
    printf("  支持多种设备\n\n");
    printf("NVMe:\n");
    printf("  专门为闪存设计\n");
    printf("  更低延迟\n");
    printf("  更高并行度\n");

    return 0;
}
```

*原来"SCSI"是"存储"的"通用语言"。*

---

## 二

林小源在研究 SCSI 的过程中，理解了它的层次结构。

SCSI 分为上层（设备驱动）、中层（核心）、下层（适配器驱动）。这种分层设计让不同的存储设备可以通过相同的接口访问。

*原来"分层"是"通用"的"方式"。*

---

## 三

林小源在观察 SCSI 的过程中，理解了它与 NVMe 的关系。

NVMe 是为闪存设计的新协议——比 SCSI 更高效。但在 Linux 中，很多存储设备仍然通过 SCSI 子系统访问。

*原来"新"和"旧"可以"共存"。*

---

### 道藏笔记

::: tip 内核启示
**SCSI 是 Linux 存储的通用框架。**

SCSI 的层次：
- 上层 — sd（磁盘）、sr（光驱）、st（磁带）
- 中层 — SCSI 核心
- 下层 — host adapter 驱动

SCSI 的意义：
- 统一的存储接口
- 支持多种设备和传输方式

*SCSI 是"通用语言"——让存储设备统一访问。*
:::

---

<ChapterGate
  id="scsi"
  title="SCSI 之试"
  prompt="SCSI 子系统的层次结构是什么？SCSI 的意义是什么？"
  answer="上层是设备驱动（sd/sr/st），中层是 SCSI 核心，下层是适配器驱动。SCSI 提供统一的存储接口，支持多种设备"
  placeholder="描述 SCSI"
/>

<ChapterNav prev="/novel/vol7-drivers/ch146" next="/novel/vol7-drivers/ch148" />
