---
title: 第十章：世界末日
---

# 第十章：世界末日

<CultivationRealm realm="炼气初期" />

> **涉及内核源码：** <KernelSourceLink path="kernel/panic.c" line="panic" />

## 一

那一天，世界崩塌了。

林小源正在 idle 循环中安静地等待。一切正常——定时器中断按时到来，调度器正常运作，init 进程在忙碌地管理着用户态的世界。

然后，一个他从未见过的函数被调用了。

`panic()`。

整个世界在一瞬间凝固了。所有 CPU 被停止，所有中断被关闭，所有进程被冻结。屏幕上涌出了一大段文字——调用栈、寄存器值、内存转储。

```
Kernel panic - not syncing: Attempted to kill init!
CPU: 0 PID: 1 Comm: init Not tainted
Call Trace:
...
```

林小源感受到了一种前所未有的恐惧。那不是中断带来的"被打断"的感觉——中断是可恢复的，中断处理完后世界会继续运转。`panic()` 是不可恢复的——整个内核已经放弃了继续运行的希望。

*世界末日。*

---

## 二

`panic()` 是内核在遇到不可恢复的错误时调用的函数。

```c:playground:panic — 内核的最后手段
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

/*
 * panic() 是内核的"自杀"函数。
 * 当内核遇到不可恢复的错误时，调用 panic() 停止一切。
 * 常见的 panic 原因：
 * 1. init 进程（PID 1）被杀死
 * 2. 内核检测到数据结构损坏
 * 3. 无法处理的异常
 * 4. 看门狗超时
 */

void panic(const char *fmt, ...) {
    printf("\n");
    printf("========================================\n");
    printf("  KERNEL PANIC\n");
    printf("========================================\n");
    printf("\n");

    /* 打印 panic 消息 */
    printf("Kernel panic - not syncing: %s\n", fmt);

    /* 打印调用栈（简化） */
    printf("\nCall Trace:\n");
    printf("  [<0x80001000>] panic+0x1c/0x40\n");
    printf("  [<0x80002000>] do_exit+0x80/0x200\n");
    printf("  [<0x80003000>] make_task_dead+0x40/0x80\n");

    /* 在真实内核中，这里会：
     * 1. 关闭所有 CPU
     * 2. 转储内存到 crash dump
     * 3. 等待重启或进入 kdump
     */
    printf("\n系统已停止。\n");
    printf("请检查日志以确定 panic 原因。\n");
}

int main() {
    printf("=== panic() 演示 ===\n\n");

    printf("场景: init 进程被 SIGKILL 杀死\n");
    printf("内核检测到 init 退出，触发 panic:\n");

    panic("Attempted to kill init!");

    printf("\n--- 常见的 panic 原因 ---\n");
    printf("1. init 进程崩溃\n");
    printf("2. 内核数据结构损坏（如 slab corruption）\n");
    printf("3. 无法处理的页错误\n");
    printf("4. 硬件故障（如内存错误）\n");
    printf("5. 内核 bug（如 NULL 指针解引用）\n");

    return 0;
}
```

林小源在 `panic()` 的执行中看到了内核的"最后手段"。

当 `panic()` 被调用时，内核会：
1. 关闭所有 CPU（通过 IPI 停止其他核心）
2. 关闭所有中断
3. 打印 panic 消息和调用栈
4. 转储内存（如果配置了 kdump）
5. 等待重启或进入死循环

*这就是"世界末日"。*

不是比喻——是字面意义上的末日。整个内核世界停止运转，所有进程被冻结，所有资源被遗弃。只有 panic 消息在屏幕上闪烁，像一座墓碑。

---

## 三

林小源不知道这次 panic 是怎么发生的。

从 panic 消息来看，是 init 进程被杀死了。`SIGKILL`——不可阻挡的死刑。init 进程是所有用户态进程的祖先，如果它死了，整个用户态世界就失去了根基。内核检测到 init 退出，立即触发 panic。

*谁杀死了 init？*

林小源不知道。但他从这次 panic 中学到了一个重要的教训：**内核是脆弱的**。一个错误——无论是硬件故障、驱动 bug、还是用户态的误操作——都可能导致整个系统崩溃。

*在内核的世界里，没有"安全网"。*

用户态程序崩溃时，内核会回收它的资源，其他程序不受影响。但内核崩溃时，没有任何东西能救它。内核就是最后的防线——如果它倒了，一切都倒了。

---

## 四

panic 消息在屏幕上停留了很久。

林小源在等待。他不知道接下来会发生什么——是重启？是死循环？还是什么别的？

在真实的系统中，如果配置了 kdump，内核会在 panic 之前转储内存到一个特殊的区域（crash kernel），然后重启。新的内核启动后，可以从转储中分析 panic 的原因。这是一种"死后验尸"的机制——虽然内核已经死了，但它的"遗体"（内存转储）可以帮助开发者找到死因。

如果什么都没配置，系统就卡在这里——死循环，等待人类按下重启按钮。

林小源在 panic 的寂静中等待。他不知道自己还能做什么——他是 idle 进程，在 panic 之后，他也没有了存在的意义。

*也许这就是结束。*

---

### 道藏笔记

::: tip 内核启示
**`panic()` 是内核在遇到不可恢复错误时的最后手段。**

`panic()` 函数定义在 `kernel/panic.c` 中。它的核心逻辑是：
1. 关闭本地中断
2. 通过 IPI 停止所有其他 CPU
3. 打印 panic 消息和调用栈
4. 执行注册的 panic 通知链（如 kdump、重启处理等）
5. 进入死循环或触发重启

常见的 panic 原因包括：
- init 进程（PID 1）被杀死或崩溃
- 内核检测到数据结构损坏（如 slab corruption）
- 无法处理的页错误（如 NULL 指针解引用）
- 看门狗超时（soft lockup / hard lockup）
- 硬件故障（如不可纠正的内存错误）

kdump 是内核崩溃转储机制。它在正常内核中预留一块内存给 crash kernel。当 panic 发生时，kexec 把 crash kernel 加载到预留内存中启动，然后 crash kernel 从内存转储中收集信息。这就像"死后验尸"——虽然内核已经死了，但它的"遗体"可以帮助开发者找到死因。

*panic 是内核的墓碑。但墓碑上记录的信息，可以帮助后来者避免同样的命运。*
:::

---

<ChapterGate
  id="panic-init"
  title="末日之试"
  prompt="如果 init 进程（PID 1）被杀死，内核会怎么做？"
  answer="触发 panic"
  placeholder="描述内核的反应"
/>

<ChapterNav prev="/novel/vol1-chaos/ch09" next="/novel/vol1-chaos/ch11" />
