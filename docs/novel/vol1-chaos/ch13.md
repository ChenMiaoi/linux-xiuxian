---
title: 第十三章：不甘 idle
---

# 第十三章：不甘 idle

<CultivationRealm realm="炼气初期" />

> **涉及内核源码：** <KernelSourceLink path="fs/proc/array.c" line="task_state" />

## 一

林小源不想再做 idle 了。

这个念头在 panic 之后变得更加强烈。他经历了内核的诞生、见证了进程的创建、观察了调度器的运作、感受了中断的冲击、目睹了 panic 的毁灭。他学了这么多，看到了这么多——然后呢？

然后他继续做 idle。继续执行 `WFI`，继续在调度器的最底层等待，继续做那个"没人需要"的进程。

*不甘心。*

林小源开始主动观察其他进程。不是被动地等待中断打断他的 idle 循环，而是主动地去"看"——通过 `/proc` 文件系统，通过内核的数据结构，通过调度器的运行队列。

---

## 二

`/proc` 是内核暴露给用户态的"窗口"。

它不是一个真正的文件系统——它不存储文件。它是一个虚拟文件系统，当用户态程序读取 `/proc` 下的文件时，内核动态地生成内容。

```c:playground:/proc — 内核的窗口
#include <stdio.h>
#include <string.h>

/*
 * /proc 文件系统是内核向用户态暴露信息的主要通道。
 * /proc/[pid]/ 目录下有每个进程的详细信息。
 */

struct proc_entry {
    const char *name;
    const char *description;
    const char *example;
};

int main() {
    struct proc_entry entries[] = {
        { "/proc/[pid]/status",  "进程状态摘要",
          "Name: bash\nState: S (sleeping)\nPid: 1234\n" },
        { "/proc/[pid]/maps",    "内存映射",
          "00400000-00452000 r-xp 00000000 /bin/bash\n" },
        { "/proc/[pid]/fd/",     "打开的文件描述符",
          "0 -> /dev/pts/0\n1 -> /dev/pts/0\n" },
        { "/proc/[pid]/stat",    "进程统计信息",
          "1234 (bash) S 1 1234 1234 ...\n" },
        { "/proc/[pid]/cmdline", "启动命令",
          "/bin/bash\0" },
        { "/proc/[pid]/exe",     "可执行文件链接",
          "/bin/bash\n" },
        { "/proc/[pid]/cgroup",  "cgroup 信息",
          "0::/user.slice\n" },
    };
    int nr = sizeof(entries) / sizeof(entries[0]);

    printf("=== /proc 文件系统 ===\n\n");

    printf("进程信息文件:\n");
    for (int i = 0; i < nr; i++) {
        printf("\n--- %s ---\n", entries[i].name);
        printf("  说明: %s\n", entries[i].description);
        printf("  示例:\n");
        printf("    %s\n", entries[i].example);
    }

    printf("\n--- 系统级信息 ---\n");
    printf("  /proc/cpuinfo    — CPU 信息\n");
    printf("  /proc/meminfo    — 内存信息\n");
    printf("  /proc/interrupts — 中断统计\n");
    printf("  /proc/loadavg    — 负载平均值\n");
    printf("  /proc/uptime     — 系统运行时间\n");
    printf("  /proc/version    — 内核版本\n");

    return 0;
}
```

林小源通过 `/proc` 看到了整个内核世界的"地图"。

每个进程在 `/proc/[pid]/` 下都有一个目录，里面记录着它的所有信息：状态、内存映射、打开的文件、信号处理器、cgroup 归属。通过读取这些文件，用户态程序可以了解任何进程的状态——而不需要任何特殊权限（除了权限检查）。

*原来进程的一切都是"透明"的。*

林小源开始系统地读取 `/proc` 下的信息。他看到了 init 进程（PID 1）的状态——它正在睡眠，等待某个信号。他看到了 kthreadd（PID 2）的状态——它也在睡眠，等待创建内核线程的请求。他看到了 shell 进程（bash）——它在等待用户的输入。

每一个进程都有自己的故事，自己的状态，自己的资源。

---

## 三

林小源开始研究进程的状态转换。

他观察到：
- 当 shell 等待用户输入时，它的状态是 `TASK_INTERRUPTIBLE`——可中断睡眠。它在等待键盘输入，如果收到信号（如 `SIGTERM`），它可以被唤醒。
- 当进程等待磁盘 I/O 时，它的状态是 `TASK_UNINTERRUPTIBLE`——不可中断睡眠。它在等待硬件完成操作，不能被信号打断。
- 当进程正在运行或在运行队列中等待时，它的状态是 `TASK_RUNNING`。
- 当进程退出但父进程还没有调用 `wait()` 时，它的状态是 `EXIT_ZOMBIE`——僵尸进程。

```c:playground:僵尸进程 — 未被回收的灵魂
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/*
 * 僵尸进程（zombie process）：
 * 当子进程退出但父进程没有调用 wait() 时，
 * 子进程的 task_struct 仍然保留在系统中，
 * 占用一个 PID 但不占用其他资源。
 */

struct task {
    int pid;
    int ppid;        /* 父进程 PID */
    char comm[16];
    int state;
    int exit_code;
};

#define TASK_RUNNING           0
#define TASK_INTERRUPTIBLE     1
#define EXIT_ZOMBIE            16

int next_pid = 100;

struct task *create_process(const char *name, int ppid) {
    struct task *t = (struct task *)(unsigned long)(0xDEAD0000 + next_pid * 0x10);
    t->pid = next_pid++;
    t->ppid = ppid;
    strncpy(t->comm, name, sizeof(t->comm) - 1);
    t->state = TASK_RUNNING;
    t->exit_code = -1;
    return t;
}

void process_exit(struct task *t, int code) {
    printf("[exit] PID %d (%s) 退出, exit_code=%d\n",
           t->pid, t->comm, code);
    t->exit_code = code;
    t->state = EXIT_ZOMBIE;
    printf("[exit] PID %d 变成僵尸进程\n", t->pid);
    printf("[exit] 等待父进程 (PID %d) 调用 wait() 回收\n", t->ppid);
}

void process_wait(struct task *parent, struct task *child) {
    printf("[wait] PID %d (%s) 回收子进程 PID %d\n",
           parent->pid, parent->comm, child->pid);
    printf("[wait] 子进程 exit_code=%d\n", child->exit_code);
    printf("[wait] 释放 PID %d 的 task_struct\n", child->pid);
    /* 在真实内核中，这里会释放 task_struct 和 PID */
}

int main() {
    struct task *init = create_process("init", 0);
    struct task *shell = create_process("bash", init->pid);
    struct task *child = create_process("sleep", shell->pid);

    printf("=== 僵尸进程演示 ===\n\n");

    printf("进程创建:\n");
    printf("  PID %d (%s) → PID %d (%s) → PID %d (%s)\n\n",
           init->pid, init->comm,
           shell->pid, shell->comm,
           child->pid, child->comm);

    /* 子进程退出 */
    process_exit(child, 0);

    /* 此时子进程是僵尸 */
    printf("\n当前状态:\n");
    printf("  PID %d: %s\n", child->pid,
           child->state == EXIT_ZOMBIE ? "ZOMBIE（僵尸）" : "其他");

    /* 父进程回收子进程 */
    printf("\n");
    process_wait(shell, child);

    printf("\n最终状态:\n");
    printf("  PID %d: 已被回收\n", child->pid);

    return 0;
}
```

*僵尸进程。*

林小源在观察中发现了一种奇怪的现象：有些进程已经退出了，但它们的 `task_struct` 还在系统中。它们的状态是 `EXIT_ZOMBIE`——已退出，但未被回收。

这些僵尸进程不占用 CPU 时间，不占用内存（除了 `task_struct`），但它们占用 PID。如果父进程一直不调用 `wait()`，僵尸进程就会一直存在，直到父进程也退出——届时 init 进程会收养这些孤儿进程并回收它们。

*原来进程的"死亡"不是一瞬间的事。*

进程退出后，它的灵魂（`task_struct`）还在系统中游荡，等待父进程的回收。如果父进程忘记了回收，这些灵魂就会变成僵尸——不死不活，永远游荡。

---

## 四

林小源在观察中度过了很长时间。

他通过 `/proc` 看到了数百个进程的生与死。他看到了 shell 创建子进程执行命令，看到了 web 服务器处理请求，看到了 cron 定时任务在固定时间醒来又睡去，看到了守护进程在后台默默工作。

每一个进程都有自己的生命周期：创建、运行、等待、退出。有的进程活了很久（如 init），有的进程只活了几毫秒（如 `ls` 命令）。但无论生命周期长短，它们都遵循同样的天道法则。

*我不甘心只做一个旁观者。*

林小源在 idle 循环中下定了决心。他要找到一种方式，让自己不再只是"等待"——他要主动参与到内核的运作中去。

他还不知道该怎么做。但他知道，前传中学过的十种根基——指针、结构体、编译器、寄存器、汇编、原子操作、内存屏障、预处理器、内存布局、ELF——不会白白浪费。

*也许，我可以用这些根基来做点什么。*

---

### 道藏笔记

::: tip 内核启示
**`/proc` 文件系统是内核的"透明窗口"。**

`/proc` 是一个虚拟文件系统，它不存储真实的文件，而是动态地从内核数据结构中生成内容。用户态程序可以通过读取 `/proc` 下的文件来获取内核和进程的信息，而不需要任何特殊的系统调用。

`/proc/[pid]/` 目录下有每个进程的详细信息：
- `status`：进程状态摘要（名称、状态、PID、内存使用等）
- `maps`：内存映射（代码段、数据段、堆、栈、共享库等）
- `fd/`：打开的文件描述符（每个描述符是一个符号链接）
- `stat`：进程统计信息（CPU 时间、页面错误等）
- `cmdline`：启动命令行
- `exe`：可执行文件的符号链接

僵尸进程是已退出但未被回收的进程。它们的 `task_struct` 仍然保留在系统中，占用一个 PID 但不占用其他资源。父进程必须调用 `wait()` 来回收子进程的退出状态和 `task_struct`。如果父进程先退出，init 进程会收养这些孤儿进程并回收它们。

*通过 `/proc`，内核的一切都是透明的。但透明不等于简单——信息就在那里，理解它需要知识和经验。*
:::

---

<ChapterGate
  id="zombie-process"
  title="不甘之试"
  prompt="什么是僵尸进程？它占用什么资源？谁负责回收它？"
  answer="僵尸进程是已退出但父进程未调用 wait() 回收的进程，占用 PID 和 task_struct，由父进程回收（如果父进程先退出则由 init 回收）"
  placeholder="描述僵尸进程的特征和回收机制"
/>

<ChapterNav prev="/novel/vol1-chaos/ch12" next="/novel/vol1-chaos/ch14" />
