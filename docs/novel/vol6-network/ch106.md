---
title: 第一百零六章：望海
---

# 第一百零六章：望海

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/socket.c" line="sys_socket" />

## 一

林小源来到了网络之海的岸边。

在这片海上，每一艘船都是一个 socket——它是网络通信的端点。进程通过 socket 发送和接收数据，就像船通过港口装卸货物。

```c:playground:socket — 网络通信的端点
#include <stdio.h>

/*
 * socket 是网络通信的抽象：
 *
 *   进程 A                     进程 B
 *     ↓                          ↓
 *   socket()                   socket()
 *     ↓                          ↓
 *   bind()                     bind()
 *     ↓                          ↓
 *   listen()        ←→         connect()
 *     ↓                          ↓
 *   accept()                   send()
 *     ↓                          ↓
 *   recv()                     close()
 *
 * socket 的系统调用：
 *   socket() — 创建 socket
 *   bind() — 绑定地址
 *   listen() — 监听连接
 *   accept() — 接受连接
 *   connect() — 发起连接
 *   send()/recv() — 发送/接收数据
 *   close() — 关闭 socket
 */

int main() {
    printf("=== socket — 网络通信的端点 ===\n\n");

    printf("socket 的生命周期:\n\n");

    printf("1. 创建:\n");
    printf("   int fd = socket(AF_INET, SOCK_STREAM, 0);\n\n");

    printf("2. 绑定地址:\n");
    printf("   struct sockaddr_in addr = {\n");
    printf("     .sin_family = AF_INET,\n");
    printf("     .sin_port = htons(8080),\n");
    printf("     .sin_addr.s_addr = INADDR_ANY,\n");
    printf("   };\n");
    printf("   bind(fd, &addr, sizeof(addr));\n\n");

    printf("3. 监听 (服务器):\n");
    printf("   listen(fd, 128);\n\n");

    printf("4. 接受连接 (服务器):\n");
    printf("   int client = accept(fd, &client_addr, &len);\n\n");

    printf("5. 连接 (客户端):\n");
    printf("   connect(fd, &server_addr, sizeof(server_addr));\n\n");

    printf("6. 通信:\n");
    printf("   send(fd, buf, len, 0);\n");
    printf("   recv(fd, buf, len, 0);\n\n");

    printf("7. 关闭:\n");
    printf("   close(fd);\n\n");

    printf("--- socket 的类型 ---\n");
    printf("SOCK_STREAM: 可靠、有序、双向（TCP）\n");
    printf("SOCK_DGRAM:  无连接、不可靠（UDP）\n");
    printf("SOCK_RAW:    原始 socket\n\n");

    printf("--- 地址族 ---\n");
    printf("AF_INET:  IPv4\n");
    printf("AF_INET6: IPv6\n");
    printf("AF_UNIX:  Unix domain socket\n");

    return 0;
}
```

*原来"海"的"入口"是 socket。*

---

## 二

林小源在研究 socket 的过程中，理解了它的分层设计。

socket 是传输层的抽象——它隐藏了底层协议的细节。无论是 TCP 还是 UDP，用户都通过相同的 socket 接口通信。

*原来"抽象"让"复杂"变"简单"。*

---

## 三

林小源在观察 socket 的过程中，理解了客户端-服务器模型。

服务器先创建 socket、绑定地址、监听连接，然后等待客户端连接。客户端创建 socket 后直接连接服务器。这就是网络通信的基本模式。

*原来"通信"是"握手"的过程。*

---

### 道藏笔记

::: tip 内核启示
**socket 是网络通信的端点。**

socket 的系统调用：
- `socket()` — 创建 socket
- `bind()` — 绑定地址
- `listen()` — 监听连接
- `accept()` — 接受连接
- `connect()` — 发起连接
- `send()/recv()` — 发送/接收数据
- `close()` — 关闭 socket

socket 的类型：
- `SOCK_STREAM` — TCP（可靠、有序）
- `SOCK_DGRAM` — UDP（无连接、不可靠）
- `SOCK_RAW` — 原始 socket

*socket 是"船"——网络通信的载体。*
:::

---

<ChapterGate
  id="socket-basics"
  title="socket 之试"
  prompt="socket 的基本系统调用有哪些？客户端-服务器模型的工作流程是什么？"
  answer="基本调用包括 socket、bind、listen、accept、connect、send、recv、close。服务器先 bind+listen+accept，客户端 connect 后双方通信"
  placeholder="描述 socket 基础"
/>

<ChapterNav prev="/novel/vol5-filesystem/ch100" next="/novel/vol6-network/ch107" />
