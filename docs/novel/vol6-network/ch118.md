---
title: 第一百一十八章：重传
---

# 第一百一十八章：重传

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/ipv4/tcp_output.c" line="tcp_retransmit_skb" />

## 一

林小源在研究 TCP 的过程中，理解了重传的机制。

网络不可靠——数据包可能丢失、重复、乱序。TCP 通过序列号、确认和重传来保证可靠性。

```c:playground:TCP 重传 — 丢失恢复
#include <stdio.h>

/*
 * TCP 重传机制：
 *
 * 1. 超时重传
 *    发送数据后启动定时器
 *    超时未收到 ACK 则重传
 *    RTO (Retransmission Timeout) 动态调整
 *
 * 2. 快重传
 *    收到 3 个重复 ACK
 *    不等超时，立即重传
 *
 * 3. SACK (选择性确认)
 *    接收方告知收到了哪些数据
 *    发送方只重传丢失的数据
 *
 * RTO 计算:
 *   SRTT — 平滑的往返时间
 *   RTTVAR — 往返时间方差
 *   RTO = SRTT + 4 * RTTVAR
 */

int main() {
    printf("=== TCP 重传 — 丢失恢复 ===\n\n");

    printf("重传机制:\n\n");

    printf("1. 超时重传:\n");
    printf("   发送方:\n");
    printf("   [seq=1] → [seq=2] → [seq=3]\n");
    printf("              ↓ 丢失\n");
    printf("   超时后重传 [seq=2]\n\n");

    printf("2. 快重传:\n");
    printf("   发送方:\n");
    printf("   [seq=1] → [seq=2] → [seq=3] → [seq=4]\n");
    printf("              ↓ 丢失\n");
    printf("   接收方: ACK=2, ACK=2, ACK=2 (重复)\n");
    printf("   发送方: 立即重传 [seq=2]\n\n");

    printf("3. SACK:\n");
    printf("   接收方告知收到的数据范围:\n");
    printf("   SACK: 3-4 (收到 3 和 4)\n");
    printf("   发送方只重传 2\n\n");

    printf("--- RTO 计算 ---\n");
    printf("SRTT: 平滑的往返时间\n");
    printf("RTTVAR: 往返时间方差\n");
    printf("RTO = SRTT + 4 * RTTVAR\n\n");

    printf("--- 超时时间 ---\n");
    printf("初始 RTO: 1 秒\n");
    printf("最大 RTO: 120 秒\n");
    printf("每次超时: RTO 翻倍\n\n");

    printf("--- 重传的问题 ---\n");
    printf("1. 延迟: 超时时间长\n");
    printf("2. 重复: 可能重传已收到的数据\n");
    printf("3. 效率: 只能重传一个包\n\n");

    printf("--- 查看重传统计 ---\n");
    printf("netstat -s | grep retrans\n");
    printf("  segments retransmitted: 123\n");

    return 0;
}
```

*原来"重传"是"可靠性"的"保障"。*

---

## 二

林小源在研究重传的过程中，理解了 RTO 的动态调整。

RTO 不是固定的——它根据实际的往返时间动态调整。如果网络延迟稳定，RTO 较小；如果网络抖动大，RTO 较大。

*原来"超时"是"动态"的。*

---

## 三

林小源在观察重传的过程中，理解了 SACK 的优势。

没有 SACK 时，接收方只能确认连续收到的数据。有了 SACK，接收方可以告知收到了哪些不连续的数据——发送方只重传真正丢失的数据。

*原来"信息"是"效率"的"来源"。*

---

### 道藏笔记

::: tip 内核启示
**TCP 重传保证数据可靠到达。**

重传机制：
- 超时重传 — RTO 超时后重传
- 快重传 — 收到 3 个重复 ACK 立即重传
- SACK — 选择性确认，只重传丢失的数据

RTO 计算：
- `RTO = SRTT + 4 * RTTVAR`
- 根据实际往返时间动态调整

*重传是"可靠性"的保障——让丢失的数据重新到达。*
:::

---

<ChapterGate
  id="retransmit"
  title="重传之试"
  prompt="TCP 有哪些重传机制？SACK 的作用是什么？"
  answer="超时重传（RTO 超时后重传）、快重传（3 个重复 ACK 立即重传）。SACK 让接收方告知收到了哪些数据，发送方只重传丢失的"
  placeholder="描述 TCP 重传"
/>

<ChapterNav prev="/novel/vol6-network/ch117" next="/novel/vol6-network/ch119" />
