---
title: 第一百零九章：信鸽
---

# 第一百零九章：信鸽

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/ipv4/udp.c" line="udp_sendmsg" />

## 一

林小源在港口看到了一群信鸽——UDP。

UDP（User Datagram Protocol）是轻量级的传输协议。它不建立连接、不保证可靠、不保证顺序——但速度快、开销小。

```c:playground:UDP — 迅捷的信鸽
#include <stdio.h>

/*
 * UDP 的特点：
 *
 * 1. 无连接
 *    不需要建立连接
 *    直接发送数据
 *
 * 2. 不可靠
 *    不保证到达
 *    不保证顺序
 *    可能重复
 *
 * 3. 消息式
 *    每次发送是独立的消息
 *    有消息边界
 *
 * 4. 快速
 *    没有握手开销
 *    没有确认开销
 *    没有重传开销
 *
 * UDP 的用途：
 *   DNS 查询 — 简单请求-响应
 *   视频流 — 可以容忍丢失
 *   游戏 — 延迟比可靠性更重要
 *   DHCP — 广播
 *
 * UDP 报文头：
 *   源端口 | 目的端口
 *   长度   | 校验和
 *   数据...
 */

int main() {
    printf("=== UDP — 迅捷的信鸽 ===\n\n");

    printf("UDP 的特点:\n");
    printf("  无连接: 不需要握手\n");
    printf("  不可靠: 不保证到达\n");
    printf("  消息式: 有消息边界\n");
    printf("  快速: 没有额外开销\n\n");

    printf("--- UDP vs TCP ---\n");
    printf("%-15s %-15s %-15s\n",
           "特性", "TCP", "UDP");
    printf("%-15s %-15s %-15s\n",
           "---", "---", "---");
    printf("%-15s %-15s %-15s\n",
           "连接", "面向连接", "无连接");
    printf("%-15s %-15s %-15s\n",
           "可靠", "是", "否");
    printf("%-15s %-15s %-15s\n",
           "顺序", "保证", "不保证");
    printf("%-15s %-15s %-15s\n",
           "边界", "无", "有");
    printf("%-15s %-15s %-15s\n",
           "速度", "较慢", "快");
    printf("%-15s %-15s %-15s\n",
           "开销", "高", "低\n");

    printf("--- UDP 的用途 ---\n");
    printf("DNS 查询:\n");
    printf("  简单的请求-响应\n");
    printf("  不需要建立连接\n\n");
    printf("视频流:\n");
    printf("  可以容忍少量丢失\n");
    printf("  延迟比可靠性更重要\n\n");
    printf("游戏:\n");
    printf("  实时性要求高\n");
    printf("  旧数据不如新数据重要\n\n");

    printf("--- UDP 报文头 ---\n");
    printf("┌────────────────────────────────┐\n");
    printf("│ 源端口 (16) │ 目的端口 (16)  │\n");
    printf("├────────────────────────────────┤\n");
    printf("│ 长度 (16)   │ 校验和 (16)    │\n");
    printf("├────────────────────────────────┤\n");
    printf("│ 数据...                        │\n");
    printf("└────────────────────────────────┘\n");
    printf("报文头只有 8 字节\n");

    return 0;
}
```

*原来"信鸽"的"快"来自"简单"。*

---

## 二

林小源在研究 UDP 的过程中，理解了它的适用场景。

UDP 适合"丢了也无所谓"的场景——视频流中丢几帧不影响观看，游戏中旧的位置信息不如新的重要。但对于需要可靠传输的场景（如文件传输），TCP 是更好的选择。

*原来"选择"取决于"需求"。*

---

## 三

林小源在观察 UDP 的过程中，理解了消息边界的意义。

TCP 是流式传输——发送 100 字节和 200 字节，接收方可能收到 300 字节的连续数据。UDP 是消息式——发送 100 字节，接收方就收到 100 字节的消息。

*原来"边界"是"消息"的"包装"。*

---

### 道藏笔记

::: tip 内核启示
**UDP 是轻量级的传输协议。**

UDP 的特点：
- 无连接 — 不需要握手
- 不可靠 — 不保证到达和顺序
- 消息式 — 有消息边界
- 快速 — 没有额外开销

UDP 的用途：
- DNS 查询 — 简单请求-响应
- 视频流 — 可以容忍丢失
- 游戏 — 延迟比可靠性重要

UDP vs TCP：
- UDP — 快但不可靠
- TCP — 可靠但有开销

*UDP 是"信鸽"——快但不保证到达。*
:::

---

<ChapterGate
  id="udp"
  title="UDP 之试"
  prompt="UDP 和 TCP 有什么区别？UDP 适合什么场景？"
  answer="UDP 无连接、不可靠、消息式、快速。适合 DNS 查询、视频流、游戏等对延迟敏感但可以容忍丢失的场景"
  placeholder="描述 UDP"
/>

<ChapterNav prev="/novel/vol6-network/ch108" next="/novel/vol6-network/ch110" />
