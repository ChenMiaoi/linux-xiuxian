---
title: 第一百二十六章：海防
---

# 第一百二十六章：海防

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/netfilter/core.c" />

## 一

林小源在网络之海中发现了防御工事——netfilter 和 iptables。

netfilter 是内核的防火墙框架——它允许在数据包经过内核时进行过滤、修改、丢弃。iptables 是用户空间的配置工具。

```c:playground:netfilter — 网络防火墙
#include <stdio.h>

/*
 * netfilter 的钩子点:
 *
 *   PREROUTING → FORWARD → POSTROUTING
 *        ↓                  ↑
 *     INPUT → 本地进程 → OUTPUT
 *
 * 链 (chains):
 *   PREROUTING — 数据包进入时
 *   INPUT — 发往本机
 *   FORWARD — 转发
 *   OUTPUT — 本机发出
 *   POSTROUTING — 数据包离开时
 *
 * 表 (tables):
 *   filter — 过滤（默认）
 *   nat — 地址转换
 *   mangle — 修改数据包
 *
 * 规则 (rules):
 *   匹配条件 → 动作
 *   -s 192.168.1.0/24 -j ACCEPT
 */

int main() {
    printf("=== netfilter — 网络防火墙 ===\n\n");

    printf("netfilter 钩子点:\n\n");
    printf("  PREROUTING → FORWARD → POSTROUTING\n");
    printf("       ↓                  ↑\n");
    printf("    INPUT → 本地进程 → OUTPUT\n\n");

    printf("--- 链 ---\n");
    printf("PREROUTING: 数据包进入时\n");
    printf("INPUT: 发往本机\n");
    printf("FORWARD: 转发\n");
    printf("OUTPUT: 本机发出\n");
    printf("POSTROUTING: 数据包离开时\n\n");

    printf("--- 表 ---\n");
    printf("filter: 过滤（默认）\n");
    printf("  ACCEPT — 允许\n");
    printf("  DROP — 丢弃\n");
    printf("  REJECT — 拒绝\n\n");
    printf("nat: 地址转换\n");
    printf("  SNAT — 源地址转换\n");
    printf("  DNAT — 目标地址转换\n");
    printf("  MASQUERADE — 伪装\n\n");
    printf("mangle: 修改数据包\n\n");

    printf("--- iptables 命令 ---\n");
    printf("查看规则:\n");
    printf("  iptables -L -n\n\n");
    printf("允许 SSH:\n");
    printf("  iptables -A INPUT -p tcp --dport 22 -j ACCEPT\n\n");
    printf("拒绝所有:\n");
    printf("  iptables -A INPUT -j DROP\n\n");
    printf("NAT:\n");
    printf("  iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE\n\n");

    printf("--- nftables ---\n");
    printf("iptables 的继任者:\n");
    printf("  更简洁的语法\n");
    printf("  更好的性能\n");
    printf("  统一的框架\n");

    return 0;
}
```

*原来"海防"是"安全"的"屏障"。*

---

## 二

林小源在研究 netfilter 的过程中，理解了它的钩子机制。

netfilter 在数据包经过内核的多个位置设置了钩子——每个钩子可以注册处理函数。iptables 在这些钩子上设置规则，实现数据包过滤。

*原来"钩子"是"拦截"的"手段"。*

---

## 三

林小源在观察 netfilter 的过程中，理解了 NAT 的作用。

NAT（Network Address Translation）把私有 IP 地址转换为公有 IP 地址——让多个设备共享一个公网 IP。这是 IPv4 地址短缺的解决方案。

*原来"转换"是"共享"的方式。*

---

### 道藏笔记

::: tip 内核启示
**netfilter 是内核的防火墙框架。**

netfilter 钩子点：
- PREROUTING — 数据包进入时
- INPUT — 发往本机
- FORWARD — 转发
- OUTPUT — 本机发出
- POSTROUTING — 数据包离开时

表：
- filter — 过滤（ACCEPT、DROP、REJECT）
- nat — 地址转换（SNAT、DNAT）
- mangle — 修改数据包

*netfilter 是"海防"——保护系统免受网络攻击。*
:::

---

<ChapterGate
  id="netfilter"
  title="netfilter 之试"
  prompt="netfilter 的钩子点有哪些？iptables 的 filter 表有什么动作？"
  answer="钩子点包括 PREROUTING、INPUT、FORWARD、OUTPUT、POSTROUTING。filter 表的动作包括 ACCEPT（允许）、DROP（丢弃）、REJECT（拒绝）"
  placeholder="描述 netfilter"
/>

<ChapterNav prev="/novel/vol6-network/ch125" next="/novel/vol6-network/ch127" />
