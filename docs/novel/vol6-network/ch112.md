---
title: 第一百一十二章：港口
---

# 第一百一十二章：港口

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="include/linux/netdevice.h" line="struct net_device" />

## 一

林小源在网络之海的岸边看到了一个个港口——网络设备。

网络设备（如 eth0、wlan0）是数据包进出系统的通道。每个网络设备有发送队列、接收队列、中断处理等机制。

```c:playground:网络设备 — 数据包的港口
#include <stdio.h>

/*
 * 网络设备 (struct net_device) 的关键字段：
 *
 *   name — 设备名（如 eth0）
 *   mtu — 最大传输单元
 *   mac_addr — MAC 地址
 *   tx_queue — 发送队列
 *   rx_queue — 接收队列
 *
 * 网络设备的操作：
 *   ndo_open — 打开设备
 *   ndo_stop — 关闭设备
 *   ndo_start_xmit — 发送数据包
 *   ndo_set_rx_mode — 设置接收模式
 *
 * 设备类型：
 *   eth0 — 以太网
 *   wlan0 — 无线网
 *   lo — 回环
 *   tun/tap — 虚拟设备
 */

int main() {
    printf("=== 网络设备 — 数据包的港口 ===\n\n");

    printf("网络设备:\n");
    printf("  eth0: 以太网\n");
    printf("  wlan0: 无线网\n");
    printf("  lo: 回环（127.0.0.1）\n");
    printf("  tun/tap: 虚拟设备\n\n");

    printf("--- 设备属性 ---\n");
    printf("name: eth0\n");
    printf("mtu: 1500\n");
    printf("mac: 00:11:22:33:44:55\n");
    printf("ip: 192.168.1.10\n\n");

    printf("--- 设备操作 ---\n");
    printf("ndo_open: 打开设备\n");
    printf("  启用中断、分配资源\n\n");
    printf("ndo_stop: 关闭设备\n");
    printf("  禁用中断、释放资源\n\n");
    printf("ndo_start_xmit: 发送数据包\n");
    printf("  把 sk_buff 发送到网卡\n\n");

    printf("--- 发送队列 ---\n");
    printf("每个设备有发送队列:\n");
    printf("  [skb1] → [skb2] → [skb3]\n");
    printf("  网卡从队列取数据包发送\n\n");

    printf("--- 中断与轮询 ---\n");
    printf("传统模式:\n");
    printf("  每个数据包触发中断\n");
    printf("  高流量时中断风暴\n\n");
    printf("NAPI 模式:\n");
    printf("  中断触发后切换到轮询\n");
    printf("  批量处理数据包\n");
    printf("  减少中断开销\n\n");

    printf("--- 查看设备 ---\n");
    printf("ip link show\n");
    printf("  eth0: <BROADCAST,MULTICAST,UP>\n");
    printf("    link/ether 00:11:22:33:44:55\n");

    return 0;
}
```

*原来"港口"是"数据包"的"通道"。*

---

## 二

林小源在研究网络设备的过程中，理解了 MTU 的作用。

MTU（Maximum Transmission Unit）是设备能发送的最大数据包大小。以太网的 MTU 通常是 1500 字节。如果数据包超过 MTU，需要分片。

*原来"大小"有"限制"。*

---

## 三

林小源在观察网络设备的过程中，理解了 NAPI 的设计。

传统模式下，每个数据包都触发中断——高流量时会产生"中断风暴"。NAPI 在第一个中断后切换到轮询模式，批量处理数据包，减少了中断开销。

*原来"轮询"比"中断"更高效。*

---

### 道藏笔记

::: tip 内核启示
**网络设备是数据包进出系统的通道。**

网络设备的关键属性：
- `name` — 设备名（如 eth0）
- `mtu` — 最大传输单元
- `mac_addr` — MAC 地址

设备操作：
- `ndo_open/stop` — 打开/关闭设备
- `ndo_start_xmit` — 发送数据包

NAPI：
- 中断触发后切换到轮询
- 批量处理数据包
- 减少中断开销

*网络设备是"港口"——数据包的进出口。*
:::

---

<ChapterGate
  id="netdevice"
  title="网络设备之试"
  prompt="什么是 MTU？NAPI 如何减少中断开销？"
  answer="MTU 是设备能发送的最大数据包大小，以太网通常 1500 字节。NAPI 在第一个中断后切换到轮询模式，批量处理数据包"
  placeholder="描述网络设备"
/>

<ChapterNav prev="/novel/vol6-network/ch111" next="/novel/vol6-network/ch113" />
