---
title: 第一百一十一章：sk_buff
---

# 第一百一十一章：sk_buff

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="include/linux/skbuff.h" line="struct sk_buff" />

## 一

林小源在网络之海中发现了一个关键的数据结构——sk_buff。

sk_buff（socket buffer）是内核中表示网络数据包的核心结构。每个数据包都由一个 sk_buff 描述——它包含数据、元数据、协议头信息。

```c:playground:sk_buff — 网络数据包的核心
#include <stdio.h>

/*
 * struct sk_buff 的关键字段：
 *
 *   head — 数据缓冲区的起始
 *   data — 当前数据的起始
 *   tail — 当前数据的结束
 *   end — 数据缓冲区的结束
 *
 *   len — 数据长度
 *   protocol — 协议类型
 *   dev — 关联的网络设备
 *   sk — 关联的 socket
 *
 * sk_buff 的结构：
 *   ┌──────────┐
 *   │ head     │ 缓冲区起始
 *   ├──────────┤
 *   │ mac_hdr  │ MAC 头
 *   ├──────────┤
 *   │ net_hdr  │ IP 头
 *   ├──────────┤
 *   │ data     │ 当前数据
 *   ├──────────┤
 *   │ tail     │ 数据结束
 *   ├──────────┤
 *   │ end      │ 缓冲区结束
 *   └──────────┘
 *
 * sk_buff 的操作：
 *   alloc_skb — 分配
 *   kfree_skb — 释放
 *   skb_put — 添加数据到尾部
 *   skb_push — 添加数据到头部
 *   skb_pull — 移除头部数据
 */

int main() {
    printf("=== sk_buff — 网络数据包的核心 ===\n\n");

    printf("sk_buff 的结构:\n");
    printf("  ┌──────────┐\n");
    printf("  │ head     │ 缓冲区起始\n");
    printf("  ├──────────┤\n");
    printf("  │ mac_hdr  │ MAC 头 (14 字节)\n");
    printf("  ├──────────┤\n");
    printf("  │ net_hdr  │ IP 头 (20 字节)\n");
    printf("  ├──────────┤\n");
    printf("  │ data     │ 当前数据\n");
    printf("  ├──────────┤\n");
    printf("  │ tail     │ 数据结束\n");
    printf("  ├──────────┤\n");
    printf("  │ end      │ 缓冲区结束\n");
    printf("  └──────────┘\n\n");

    printf("--- sk_buff 的生命周期 ---\n");
    printf("1. 分配: alloc_skb()\n");
    printf("2. 填充数据\n");
    printf("3. 添加协议头\n");
    printf("4. 发送或接收\n");
    printf("5. 释放: kfree_skb()\n\n");

    printf("--- sk_buff 操作 ---\n");
    printf("skb_put(skb, len):\n");
    printf("  在尾部添加数据\n");
    printf("  tail 向后移动\n\n");
    printf("skb_push(skb, len):\n");
    printf("  在头部添加数据\n");
    printf("  data 向前移动\n\n");
    printf("skb_pull(skb, len):\n");
    printf("  移除头部数据\n");
    printf("  data 向后移动\n\n");

    printf("--- 协议栈的 sk_buff ---\n");
    printf("发送时:\n");
    printf("  应用层数据\n");
    printf("  → 添加 TCP 头 (skb_push)\n");
    printf("  → 添加 IP 头 (skb_push)\n");
    printf("  → 添加 MAC 头 (skb_push)\n");
    printf("  → 发送到网卡\n\n");
    printf("接收时:\n");
    printf("  网卡接收数据\n");
    printf("  → 去除 MAC 头 (skb_pull)\n");
    printf("  → 去除 IP 头 (skb_pull)\n");
    printf("  → 去除 TCP 头 (skb_pull)\n");
    printf("  → 传递给应用层\n");

    return 0;
}
```

*原来"数据包"的"身体"是 sk_buff。*

---

## 二

林小源在研究 sk_buff 的过程中，理解了它的高效设计。

sk_buff 使用 head/data/tail/end 四个指针管理缓冲区——添加或移除协议头只需要移动指针，不需要复制数据。这大大提高了性能。

*原来"指针"比"复制"更高效。*

---

## 三

林小源在观察 sk_buff 的过程中，理解了它在协议栈中的流转。

发送时，sk_buff 从应用层向下流转，每层添加自己的协议头。接收时，sk_buff 从网卡向上流转，每层移除自己的协议头。sk_buff 是协议栈的"信使"。

*原来"流转"是"分层"的"实现"。*

---

### 道藏笔记

::: tip 内核启示
**sk_buff 是内核中表示网络数据包的核心结构。**

sk_buff 的关键字段：
- head/data/tail/end — 缓冲区指针
- len — 数据长度
- protocol — 协议类型
- dev — 网络设备
- sk — socket

sk_buff 操作：
- `skb_put` — 尾部添加数据
- `skb_push` — 头部添加数据
- `skb_pull` — 移除头部数据

sk_buff 在协议栈中：
- 发送：逐层添加协议头
- 接收：逐层移除协议头

*sk_buff 是"信使"——承载数据在协议栈中流转。*
:::

---

<ChapterGate
  id="skbuff"
  title="sk_buff 之试"
  prompt="sk_buff 的 head/data/tail/end 指针有什么作用？发送时 sk_buff 如何流转？"
  answer="head/end 是缓冲区边界，data/tail 是当前数据的起止。发送时 sk_buff 从应用层向下流转，每层用 skb_push 添加协议头"
  placeholder="描述 sk_buff"
/>

<ChapterNav prev="/novel/vol6-network/ch110" next="/novel/vol6-network/ch112" />
