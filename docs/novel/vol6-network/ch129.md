---
title: 第一百二十九章：网络之道
---

# 第一百二十九章：网络之道

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/core/dev.c" line="dev_queue_xmit" />

## 一

林小源在走完网络的旅程后，站在高处回望，理解了网络子系统的核心思想。

网络的本质是"分层"——每一层都有自己的职责，对上层提供抽象，对下层隐藏细节。

```c:playground:网络之道 — 分层的艺术
#include <stdio.h>

/*
 * 网络的核心思想:
 *
 * 1. 分层
 *    应用层: HTTP, SSH
 *    传输层: TCP, UDP
 *    网络层: IP
 *    链路层: 以太网
 *    每层对上层提供抽象
 *
 * 2. 端到端原则
 *    可靠性在端点实现
 *    网络只负责传输
 *    UDP 贯彻了这个原则
 *
 * 3. 抽象
 *    socket — 通信端点
 *    sk_buff — 数据包
 *    net_device — 网络设备
 *
 * 4. 可编程
 *    eBPF — 内核中的可编程
 *    netfilter — 钩子机制
 *
 * 5. 隔离
 *    网络命名空间
 *    容器网络
 */

int main() {
    printf("=== 网络之道 — 分层的艺术 ===\n\n");

    printf("网络的核心思想:\n\n");

    printf("1. 分层:\n");
    printf("   应用层: HTTP, SSH\n");
    printf("   传输层: TCP, UDP\n");
    printf("   网络层: IP\n");
    printf("   链路层: 以太网\n\n");

    printf("2. 端到端原则:\n");
    printf("   可靠性在端点实现\n");
    printf("   网络只负责传输\n\n");

    printf("3. 抽象:\n");
    printf("   socket — 通信端点\n");
    printf("   sk_buff — 数据包\n");
    printf("   net_device — 网络设备\n\n");

    printf("4. 可编程:\n");
    printf("   eBPF — 内核中的可编程\n");
    printf("   netfilter — 钩子机制\n\n");

    printf("5. 隔离:\n");
    printf("   网络命名空间\n");
    printf("   容器网络\n\n");

    printf("--- 设计的权衡 ---\n");
    printf("可靠 vs 快速:\n");
    printf("  TCP 可靠但有开销\n");
    printf("  UDP 快速但不可靠\n\n");
    printf("安全 vs 性能:\n");
    printf("  过滤有开销\n");
    printf("  但安全更重要\n\n");
    printf("简单 vs 功能:\n");
    printf("  简单的协议更高效\n");
    printf("  但功能丰富更实用\n");

    return 0;
}
```

*原来"道"是"分层"的"艺术"。*

---

## 二

林小源在思考网络的过程中，理解了端到端原则。

端到端原则认为：可靠性等功能应该在端点实现，而不是在网络中间。这让网络保持简单，把复杂性留给需要它的应用程序。

*原来"简单"是"网络"的"哲学"。*

---

## 三

林小源在观察网络的过程中，理解了可编程的价值。

eBPF 让网络变得可编程——用户可以在内核中运行自定义程序，实现流量过滤、负载均衡、性能监控等功能。

*原来"可编程"是"未来"的"方向"。*

---

### 道藏笔记

::: tip 内核启示
**网络的本质是"分层"。**

核心思想：
- 分层 — 每层有自己的职责
- 端到端 — 可靠性在端点实现
- 抽象 — socket、sk_buff、net_device
- 可编程 — eBPF
- 隔离 — 网络命名空间

设计的权衡：
- 可靠 vs 快速
- 安全 vs 性能
- 简单 vs 功能

*网络之道是"分层"——让复杂变简单。*
:::

---

<ChapterGate
  id="net-philosophy"
  title="网络之道"
  prompt="网络的核心思想是什么？端到端原则的含义是什么？"
  answer="核心思想包括分层、端到端原则、抽象、可编程、隔离。端到端原则认为可靠性在端点实现，网络只负责传输"
  placeholder="描述网络的哲学"
/>

<ChapterNav prev="/novel/vol6-network/ch128" next="/novel/vol6-network/ch130" />
