---
title: 第一百二十八章：暗流
---

# 第一百二十八章：暗流

<CultivationRealm realm="问道期" />

> **涉及内核源码：** <KernelSourceLink path="net/ipv4/tcp_input.c" />

## 一

林小源在网络之海中发现了暗流——网络安全威胁。

网络攻击无处不在——SYN Flood、DDoS、中间人攻击、端口扫描。理解这些攻击是防御的基础。

```c:playground:网络安全 — 识别暗流
#include <stdio.h>

/*
 * 常见的网络攻击:
 *
 * 1. SYN Flood
 *    发送大量 SYN 不完成握手
 *    耗尽服务器资源
 *    防御: SYN cookies
 *
 * 2. DDoS
 *    分布式拒绝服务
 *    大量流量淹没目标
 *    防御: 流量清洗、CDN
 *
 * 3. 中间人攻击
 *    截获通信
 *    ARP 欺骗
 *    防御: HTTPS、证书验证
 *
 * 4. 端口扫描
 *    探测开放端口
 *    nmap 工具
 *    防御: 关闭不必要端口
 *
 * 5. DNS 劫持
 *    篡改 DNS 响应
 *    防御: DNSSEC
 */

int main() {
    printf("=== 网络安全 — 识别暗流 ===\n\n");

    printf("常见的网络攻击:\n\n");

    printf("1. SYN Flood:\n");
    printf("   发送大量 SYN 不完成握手\n");
    printf("   耗尽服务器资源\n");
    printf("   防御: SYN cookies\n\n");

    printf("2. DDoS:\n");
    printf("   分布式拒绝服务\n");
    printf("   大量流量淹没目标\n");
    printf("   防御: 流量清洗、CDN\n\n");

    printf("3. 中间人攻击:\n");
    printf("   截获通信\n");
    printf("   ARP 欺骗\n");
    printf("   防御: HTTPS、证书验证\n\n");

    printf("4. 端口扫描:\n");
    printf("   探测开放端口\n");
    printf("   nmap 工具\n");
    printf("   防御: 关闭不必要端口\n\n");

    printf("5. DNS 劫持:\n");
    printf("   篡改 DNS 响应\n");
    printf("   防御: DNSSEC\n\n");

    printf("--- 内核安全机制 ---\n");
    printf("SYN cookies:\n");
    printf("   不分配资源\n");
    printf("   编码在序列号中\n\n");
    printf("iptables:\n");
    printf("   过滤恶意流量\n");
    printf("   限速\n\n");
    printf("eBPF:\n");
    printf("   可编程的流量处理\n");
    printf("   DDoS 防御\n\n");

    printf("--- 安全原则 ---\n");
    printf("1. 最小权限\n");
    printf("2. 纵深防御\n");
    printf("3. 默认拒绝\n");
    printf("4. 日志审计\n");

    return 0;
}
```

*原来"暗流"是"看不见"的"威胁"。*

---

## 二

林小源在研究网络安全的过程中，理解了防御的层次。

网络安全不是单一的措施——而是多层次的防御。从网络层（iptables）到应用层（HTTPS），每层都有自己的防御手段。

*原来"纵深"是"安全"的"策略"。*

---

## 三

林小源在观察网络安全的过程中，理解了最小权限原则。

只开放必要的端口、只授予必要的权限——这减少了攻击面。默认拒绝所有流量，只允许已知的安全流量。

*原来"少"是"安全"的"来源"。*

---

### 道藏笔记

::: tip 内核启示
**网络安全需要多层次防御。**

常见攻击：
- SYN Flood — 耗尽资源
- DDoS — 流量淹没
- 中间人 — 截获通信
- 端口扫描 — 探测漏洞

防御手段：
- SYN cookies — 防御 SYN Flood
- iptables — 流量过滤
- eBPF — 可编程防御

安全原则：
- 最小权限
- 纵深防御
- 默认拒绝
- 日志审计

*网络安全是"暗流"中的"航行"——需要警惕和防御。*
:::

---

<ChapterGate
  id="net-security"
  title="网络安全之试"
  prompt="常见的网络攻击有哪些？如何防御 SYN Flood？"
  answer="常见攻击包括 SYN Flood、DDoS、中间人攻击、端口扫描。SYN Flood 通过 SYN cookies 防御——不分配资源，编码在序列号中"
  placeholder="描述网络安全"
/>

<ChapterNav prev="/novel/vol6-network/ch127" next="/novel/vol6-network/ch129" />
